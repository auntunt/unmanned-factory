__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.ruff_cache/

# 审计库：本地运行产物，不进版本控制
audit.db
*.db
*.db-journal
*.db-wal
*.db-shm

# worktree 池：并行派发的产物，人 review 完自行删
.factory-worktrees/

# playwright-mcp 的截图/快照落盘目录
.playwright-mcp/

# `--demo` 现造的队列目录：和 demo.db 一样是每次跑起来重新生成的
demo-queue/

# 前端构建产物：留在工作树里会被 shadow_code 影子代码闸门判为违规
frontend/dist/
frontend/node_modules/

# Local v3 rehearsal, including isolated sample repositories and browser evidence.
.factory-preview/
*.worker.lock

# Platform browser dependencies are installed from their lockfile.
runtime/project-browser/node_modules/
