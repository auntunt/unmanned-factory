## 契约: T06 移除旧 dashboard / console，先把领域词典搬出来

### 我的输入（依赖项）
- 项目根目录：`/home/ubuntu/workspace/unmanned-factory/`
- CLI 框架是 **argparse**（不是 click）：子命令用 `sub.add_parser(...)` + `set_defaults(func=...)`
- 实测的依赖关系（已 grep 确认）：
  - `factory/cli.py:986` → `from factory.dashboard import build_demo, build_demo_queue, export, serve`
  - `factory/cli.py:1167` → `dash = sub.add_parser("dashboard", ...)`；`:1182` → `dash.set_defaults(func=_cmd_dashboard)`
  - `tests/test_console.py:20-22` → 导入 `factory.console` / `console_read` / `console_render`
  - `tests/test_dashboard.py:26` → 导入 dashboard 的 17 个符号
  - `tests/test_gate_visibility.py:15` → `from factory.dashboard import FAULT_CLAIMS, GATE_CLAIMS`
- **没有 `console` 子命令**（只有 dashboard）；console_server 是被 dashboard 挂载的

### 关键前置：GATE_CLAIMS / FAULT_CLAIMS 不是 UI 代码
`factory/dashboard.py:68` 的 `GATE_CLAIMS`（13 道闸门 → 人话解释）和 `:98` 的
`FAULT_CLAIMS`（工具自身故障，**不许与前者合并**）是领域词典，新前端要继续用。
它们必须先搬走，不能随 dashboard 一起删。

搬迁目标：新建 `factory/gate_claims.py`，原样搬过去，**连注释一起搬**
（那些注释记录了「为什么两张表不许合并」「为什么不从源码正则抽取」，是踩过的坑）。

### 我的输出（交付物）
1. 新文件 `factory/gate_claims.py`：含 `GATE_CLAIMS`、`FAULT_CLAIMS` 及原注释
2. `git rm` 删除：
   - `factory/dashboard.py`
   - `factory/console.py`
   - `factory/console_read.py`
   - `factory/console_render.py`
   - `factory/console_server.py`
   - `tests/test_dashboard.py`（1310 行，全部测被删模块）
   - `tests/test_console.py`（129 行，全部测被删模块）
3. 修改 `factory/cli.py`：删掉 `_cmd_dashboard` 函数（:984-）、`dash = sub.add_parser("dashboard"...)`（:1167）
   及其所有 `dash.add_argument(...)` / `dash.set_defaults(...)`（到 :1182）
4. 修改 `tests/test_gate_visibility.py:15`：导入源改为 `from factory.gate_claims import FAULT_CLAIMS, GATE_CLAIMS`

### 完成标准
- [ ] `factory/gate_claims.py` 存在，`GATE_CLAIMS` 有 13 项、`FAULT_CLAIMS` 有 1 项
- [ ] 五个 factory 模块 + 两个测试文件已删（`git status` 显示 deleted）
- [ ] `uv run python -m factory.cli --help` 不再列出 `dashboard`
- [ ] 全仓无残留引用：
      `grep -rn "factory.dashboard\|factory.console" --include="*.py" .` 无输出
- [ ] `tests/test_gate_visibility.py` 全部通过（它是这次搬迁的守门测试）
- [ ] 回归：`uv run pytest tests/ -q --deselect tests/test_e2e_smoke.py` 全绿

### 验证命令

```bash
test ! -f factory/dashboard.py
test ! -f factory/console.py
test ! -f factory/console_read.py
test ! -f factory/console_render.py
test ! -f factory/console_server.py
test -f factory/gate_claims.py

# 无残留 import
! grep -rqn "factory\.dashboard\|factory\.console" --include="*.py" .

# CLI 里没有 dashboard 了
! uv run python -m factory.cli --help 2>&1 | grep -q dashboard

# 词典搬迁没搬丢
uv run python -c "from factory.gate_claims import GATE_CLAIMS, FAULT_CLAIMS; assert len(GATE_CLAIMS)==13, len(GATE_CLAIMS); assert len(FAULT_CLAIMS)==1"

# 守门测试
uv run pytest tests/test_gate_visibility.py -q
```

### 不包含（边界说明）
- 不碰 `factory/routing.py` / `routing.yaml`
- 不碰 `factory/backlog/` / `factory/audit/`（数据层，新 API 要用）
- 不改 Caddy 配置（T07 负责）
- 不实现新前端任何代码（T01-T05 负责）
- 不保留 dashboard 的 HTML 渲染逻辑 —— 新前端是 React，旧模板没有复用价值

### 技术约束
- 语言：Python 3.11+
- 用 `git rm` 而不是 `rm`
- `cli.py` 是 argparse：删子命令要同时删 `add_parser` 段、`add_argument` 段、`set_defaults`、以及 `_cmd_dashboard` 函数本体
- 搬 `gate_claims.py` 时用复制粘贴，不要「顺手改进」那两张表的内容
