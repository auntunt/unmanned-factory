# 首次同机 Docker 交付验收

## 决策

使用当前 webuddy 服务器、Docker、临时固定地址；不等待域名/DNS。用户贴的驭流 Nginx 文档仅作为流程参考，实际服务器使用 Caddy。见 [DECISION.md](DECISION.md)。

## 实际进展

| 环节 | 当前证据 |
|---|---|
| 环境与部署目标 | 当前服务器、Docker、受控 SSH 连接已核验 |
| 开发示例网页服务 | 原 Counter 迭代，真实模型生成 app.py、Dockerfile；25 项应用测试通过 |
| 独立验收 | 未通过；33 条通过，平台截图范围对账 1 条失败；另有浏览器回执编号错误 |
| 发布应用到 GitHub | 本次新网页版本尚未执行；此前 CLI Counter 发布已通过，不能混算 |
| Docker 上线与公网操作 | 尚未执行 |
| 同地址第二轮更新 | 尚未执行 |

应用 run：`88256a6c708843318c58a850938701ac`。保存的应用提交：`75b89f3da9a014e5476a23cc3a4c2a605e86d212`。平台生产仍为 `4c56632`；RC1 为 `ebf0a64`，两份里程碑标签不移动。

## 当前修复

两个真实断点及源证据见 [CLAUDE-HANDOFF.md](CLAUDE-HANDOFF.md) 与 [review-contract-failure.json](review-contract-failure.json)。Claude Desktop Message15 已发送并观察到 Running；Message17 补充文件内容绑定要求；Message19 提醒补正次数交错的问题，Claude 随后实际复现两次补正并修正。

既有旧截图事件没有生成时的内容绑定，不能事后伪造哈希或直接降低闸门使其通过。旧现场恢复路径须保留原始图片证据，并显式记录对提交的整理。

## 成本与证据边界

当前运行报告累计已知模型费用约 28.56 美元，测试项目上限已从 15 调整到 20、再到 30。保留每次失败，不宣称一次输入已无人闭环。修复期间停止重复真实模型调用，不再盲目增加预算。

现场历经 flows 格式错误、SDK 探测短暂失败、预算中止、favicon404；其修复/恢复记录见 DECISION 和本地原始报告。修复定向测试仅证明相应代码路径，不能替代 GitHub、Docker 和固定地址的实际验收。


## Codex 独立复核补充

最终代码候选：`5f25dc7ecb9c06cd6f4aff1fc0cea5e1f5628be1`，包含首修 `58b207e` 与旧观察补修 `5f25dc7`，远端分支已核对。

- macOS：截图范围对账、验收回执与Codex独立复现，15 passed / exit 0。
- Linux：相同15项，15 passed / exit 0，源代码从上述精确提交归档到独立验收目录；复用既有依赖，不变更生产服务。
- 首修曾遗漏「正确引用旧快照 + 本轮产生新clean观察」，Codex原样断言先1 failed，补修后通过。
- GitHub上的RC1与生产回退tag不移动。
- **这不是Docker交付全链通过**：旧运行中的两张截图没有生成时绑定，新规则不会事后追认；需保留原始证据后整理旧交付现场，再实际验证发布、部署、同地址更新。
- 不采用“补写事前声明”处理旧文件：既有scope规则会将晚声明判为late；也不以人工复核直接覆盖平台fail。
