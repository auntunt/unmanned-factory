# Historical attempt audit — 2026-09-09

Read-only source: `/home/ubuntu/factory-data/audit.db`, SHA-256 `abb371364607d4e8fac643b8b4c35f92785a560d0fa73aba8185cafe6ec3f2c8`. Confirmed 26 attempts and $54.85 recorded cost. These are legacy dispatcher/supervisor records, not the calculator's control-plane run ledger.

The 22 regression rejection rows classify as follows, counting each attempt once:

| Evidence category | Attempts | Associated recorded USD |
|---|---:|---:|
| Harness startup failure, timeout or aborted tools | 9 | 15.60 |
| Missing `.venv/bin/python`, exit 127 | 2 | 10.14 |
| Warning-only/truncated diagnostic evidence | 5 | 16.42 |
| No code changes produced | 3 | 3.28 |
| No checks configured | 3 | 4.96 |

Costs are associated attempt spend, not proven wasted spend. Zero-dollar entries are recorded zeros, not proof that no provider usage occurred. No test assertion failure can be established from the five incomplete records; absence of pytest output also does not establish that pytest never ran.

`RegressionSupervisor.review` executes its checks sequentially. The proposed race between those two checks is therefore not established. `check_env` did inherit the host VIRTUAL_ENV; this is now removed by default. The current diagnostic formatter preserves both stderr and stdout, so the historical warning-only records cannot be reconstructed merely by inspecting present code.

The calculator's configured check is `git diff --check HEAD`; it passed with exit zero in a fresh detached worktree at the run baseline on the server. This verifies check launch/integrity, not functional correctness of the calculator.
