# 一次输入浏览器验收 · 2026-09-16

本次是 **隔离本地脚本演练**，不是线上真实模型产品质量验收。使用真实浏览器、真实工作区创建与运行 API、Git 工作区、自动规格、执行和验收服务、成果归档与下载；模型回答由临时隔离脚本提供，不调用模型或外部服务。验收后脚本移出仓库，避免进入工程源码检查范围。

## 结果

- 首页默认仅目标文本与可选附件两个输入，无模型、助手、工作类型下拉选择。
- 默认主导航为“开始制作 / 我的作品”；“管理”展开后可访问原管理页面。
- 390 × 844 首页、运行成果页：documentElement.scrollWidth = innerWidth = 390，没有横向溢出。
- 浏览器无错误记录、无开发错误遮罩。
- 浏览器一次填写目标并点击制作，真实 POST create-workspace 与 POST runs 均返回 201。
- 成功任务：`ff9549539394408fb3d23d3e0870dd68`。持久化状态 `ready_for_review`、`source.interaction_mode=automatic`、`spec_confirmation.automatic=true`。没有点击规格确认。
- 独立脚本在独立验收工作区实际读取 welcome.txt，核对 UTF-8 内容为“工单服务已就绪”。
- 从本次任务页面点击下载成果 ZIP，并实际解包再次核对 welcome.txt 内容一致。ZIP 包含 welcome.txt 与本次规格记录。

## 发现与修正

- 移动首页主按钮因文案并排被压窄折行，已将该页移动端操作区纵向布局，按钮整行显示。
- 自动模式分析重试原固定文案称“完成后仍需确认规格”，已按本次运行 interaction_mode 区分，增加行为测试。
- 旧 preview_v3 脚本仅支持旧规划响应，不支持 Analysis schema；新建本地演练适配器支持分析响应及规范的事前范围声明。两次演练器适配失败保留在隔离数据中，不计为成功任务、不修改业务后端规避检查。

## 截图

- [桌面首页](home-desktop.png)
- [移动首页](home-mobile.png)
- [桌面成果](result-desktop.png)
- [移动成果](result-mobile.png)

以上证明交互与服务衔接、自动规格、简单文件产物与下载链条可用，不代表复杂 coding 产品已通过真实模型端到端验收。
