# CLI 职能包首轮模拟验收

运行：0e16751485ab44e6b3d7f84821907657。项目：模拟验收 · 工时 CSV 汇总 CLI。职能体：自动化 CLI 构建 v1。源提交：4aa8e97d2153b01fdad435ea1562d2b6d3034a2f。

## 结果

平台判定 ready_for_review，38 项自带测试通过。后续独立验收结论为**未完全通过原始契约**。保留原始运行记录，不改写历史 verdict。

在新的临时虚拟环境使用 uv 安装项目，从源码目录之外调用真实 hoursum 入口：

| 输入 | 实际输出 | 判断 |
| --- | --- | --- |
| 0.1 和 0.2，路径含空格 | exit 0，total_hours=0.3，row_count=2 | 通过 |
| 仅表头 | exit 0，total_hours=0，row_count=0 | 通过 |
| NaN | exit 1，stdout 空，stderr 有诊断 | 通过 |
| 0.0000001 | exit 1，提示最多 6 位小数 | 未通过：原始需求没有这项限制 |

源码还额外限制单条值最多 1e9。模型验收明确注意到这两个范围缩减，却因 README 已说明而判为 non-blocking。这不是单纯缺少测试，而是把实现说明当成了改变需求的授权。

## 执行记录

北京时间 01:02:43 创建，01:05:06 开始执行，01:19:30 完成；总计约 16 分 47 秒，开始执行后约 14 分 24 秒。

- 规划一次，开发 attempt 一次，独立验收一次；未发生任务级重启或重新规划。
- 上下文组装一次，开发请求发送一次 6703 字符。没有触发恢复路径，因此本次不能验证跨失败恢复是否减少上下文重载。
- 23 次终端命令，累计执行约 26.8 秒，2 次非零退出；安装失败在同一开发会话中修正，没有重建整个任务。开发过程中重复运行过完整小测试集，不能把所有重复调用都归为平台要求。
- 71 次工具调用。其余时间主要落在 SDK 调用/生成/网络等区间；现有记录不足以进一步分离上游排队和生成耗时。
- 1 次额外平台确认：请求中“缺少必要权限且无法在当前环境完成时报告具体阻碍”被关键词权限命中，从低风险升为高风险。确认后开发和验收都使用了 Opus。
- SDK 返回成本累计约 20.58 USD（开发约 18.73，验收约 1.85），高于项目 10 USD 配置值。这里只记录 SDK 的估算字段，未核对中转站实际计费；需要另行核查预算在长调用和验收阶段的约束，不能宣称 10 USD 已形成硬上限。

## 本次改进

风险规则仅排除明确的“缺少权限时报告/说明/询问”短语中的权限提及，保留同句其他敏感操作、敏感文件路径和模型声明高风险的检查。不采用一整句带“不”就放行的做法。已用原始触发句复现失败后修复，并验证敏感操作仍被识别。

独立验收提示增加：README 声明不构成缩减输入域的授权，具体的范围、精度、格式或平台兼容差距应判失败。此为提示改进，尚未通过新的真实模型调用证明效果。

后续优先：用该精度反例做能力迭代；检查预算控制与上游计量；再做普通 autocoding 对照。避免在成本边界未核实前自动追加多轮付费比较。原测试项目保留为未完全通过的样本，不伪造全量成功。
