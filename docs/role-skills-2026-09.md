# 岗位能力补充（2026-09）

本次是明确执行的数据更新，不在服务启动时自动改写用户岗位。能力原文与岗位映射见 `skill-expansion-2026-09.json`。

| 岗位 | 新增能力 |
| --- | --- |
| 老旧项目翻新 | 仓库地图、兼容改造、环境诊断、格式识别、接续交付 |
| 老项目渐进改造 | 仓库地图、兼容改造、环境诊断、接续交付 |
| 自动化 CLI 构建 | CLI 契约、转换映射、转换回归、接续交付 |
| 项目代码梳理 | 仓库地图、环境诊断、接续交付 |
| 能力自进化设计 | 证据评估、能力边界、接续交付 |
| 逆向工程 | 离线格式识别、转换映射、转换回归、CLI 契约、接续交付 |
| 需求分析 | 需求与验收样例、能力边界 |

执行：`PYTHONPATH=. python scripts/equip_role_skills.py --database <control.db> --actor assistant:user-request`。先备份数据库。脚本通过 ModuleStore 和 ManifestStore 添加版本与审计，保留身份、断言、已有引用及外部适配信息，以非人类权限保存清单。完全相同的技能不重复挂载。岗位匹配采用名称，重命名后的岗位需显式更新映射，不猜测。

新引用适用于后续冻结的运行，不修改历史快照。可在清单历史中恢复旧 revision。使用职能体原有 v2 导出入口即可携带新增 skill。

本次能力为平台原创方法，不安装工具、不提供不存在的执行权限，不等于业务样本已经验收。外部 reverse-skill 包仍需受控适配、独立验收和人工签署；上传库存与已装备能力是不同状态，不能用本次本地补充代替外部包签署。
