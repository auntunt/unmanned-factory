# 工作区与职能体交互复查（2026-09-09）

目标：先建立工作区，再描述需求；用户应直接领取成果，维护配置有明确入口。

修正：空工作区直接显示需求输入；简短非空口语描述进入规划，空白仍拒绝；工程总览面包屑修正；新建工作区链接直接打开创建表单。已绑定会话明确锁定项目，新对话可重新选择；隐藏系统仓库标识。任务与维护分别保留未发送的文字及附件，同模式点击不清空会话。模型在任务模式只读，通过维护入口修改。维护历史优先恢复最新任务终态，隐藏已完成任务的旧排队消息。成果页优先查看下载，GitHub 发布收进可选区域；费用尚未返回不再覆盖已验证成果的领取指引，费用记录仍保留。

验证：前端构建及 127 项测试通过。后端工作区、职能体路由、交付文件相关 30 项回归通过。浏览器在隔离本地环境实际验证空工作区、两字需求按钮、绑定项目锁定、模型只读与维护切换、草稿隔离恢复、维护完成状态、已验证成果页及 RESULT.md 预览。未在生产创建测试账号或触发付费模型运行。

## 暂停任务处理入口

执行页错误记录后提供处理意见表单，页顶和暂停说明可直接定位并聚焦。范围越界从真实失败任务及旧 artifacts 读取；口语处理意见走原有 clarify 接口，保留失败证据进入规划历史，原批准失效，仍走既有策略和范围检查。新运行作为次级选项，并说明费用及职能体冻结配置。没有自动恢复生产任务或扩大授权。

验证：前端 128 项测试；后端澄清路径 2 项、工作区/职能体/成果 30 项通过。修正既有澄清测试对旧默认监督模式的隐含依赖，显式设置监督策略。本地隔离浏览器复现相同四文件越界，检查提示、处理输入及按钮状态；生产运行只读核对。

## 回答后续执行（替代默认重新规划）

执行暂停现在走 continue 接口，保持运行 ID、计划版本、模型快照与验收范围。回复直接加入执行提示，不调用规划器。已验证任务的提交重放到新的隔离集成目录；失败任务的文件草稿带入新的受保护工作树，原现场保留。只派发未完成任务，费用累计在原运行。预算、基线变化、检查变化及并发重复提交仍会阻止继续。职能体对话中的执行暂停回复使用同一路径。

pyproject.toml 不再全文件禁止：允许常规项目字段、标准 setuptools 构建与包发现信息；测试配置、插件入口与自定义构建后端仍需恢复或单独审查。不能凭“继续”绕过范围或验收检查。

验证包含：已完成任务不重复调用模型、失败草稿保留、旧现场留存、同计划继续到交付、规划调用次数不增加、费用累计、重复提交冲突、鉴权、元数据与测试配置区分。后端相关 47 项与续执行接口 2 项通过；前端 128 项通过。运行配置中的两项旧测试显式设为监督模式，避免与默认自动执行竞争。
