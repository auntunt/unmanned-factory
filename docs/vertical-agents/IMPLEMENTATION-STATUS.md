# 实现与验收记录

2026-09-09。Luna 实现，Astra 审查、修正与集成验收。首版核心闭环已在本地完成，尚未部署生产。

## 已实现

- 职能体作为首页入口；创建、选择职能体，分别进入任务对话与维护对话。
- 维护对话读取 Skill ZIP 和口语要求，调用真实模型整理持久草稿；显示变化、冲突，应用后生成不可变版本。
- 默认及规划、执行、验证、维护阶段模型配置；运行冻结职能体版本和模型配置，重试及澄清延续原配置。
- 项目任务复用现有规划、执行、检查与独立验证流程；无项目时可先进行只读对话。
- 对话内展示真实运行状态及成果，支持预览、逐文件下载、完整归档下载。安装包必须来自真实构建成果。
- 维护任务后台运行、状态持久化、取消、重启中断说明；草稿并发冲突检测、应用幂等、版本恢复。
- 需要澄清的任务可由对话继续；执行中的补充会明确提示尚未采用，完成后发送“继续”创建后续任务。

## 真实模型与浏览器验收

使用独立本地数据库、项目与账号，调用 Codex / gpt-5.6-luna，未推送 GitHub。

1. 浏览器创建“旧项目维护员”，上传用户提供的 project-maintenance-skill.zip。原包包含 8 个文件、使用反斜杠路径，已正确解析并保存原始归档；没有执行附件脚本。
2. 维护对话加入通用维护边界及报告标记 VA-TEST-93，真实模型生成草稿，浏览器应用为 v2。
3. 创建简单文档任务，任务正文没有提供标记字符串。真实规划、执行和独立验证后，RESULT.md 包含“验收通过”以及仅在维护规则中教给它的标记。
4. 浏览器预览并实际下载 RESULT.md，下载字节与验收提交完全一致。
5. 恢复 v1 产生 v3，历史运行仍冻结 v2；再恢复 v2 内容产生 v4。重启服务后，对话、版本与成果仍可读取。

运行：3067e1cd456c4a7dbc99a17db4221868。详细哈希、模型调用与验证结论见 [LIVE-ACCEPTANCE.json](LIVE-ACCEPTANCE.json)。该成功链路 4 次真实调用共 121617 tokens；Provider 未返回美元费用，不能据此宣称费用为零或已准确计算。此前连接探测不包含在此数值中。

## 程序验证

- 最终相关后端：78 passed，覆盖存储、接口、共享运行底座、成果交付、GitHub 错误处理、自主运行及旧工作台接口。
- 前端构建成功，122 tests passed / 12 files。
- git diff --check 通过。
- 测试 Provider 用于并发、取消、额度、模型分工、冻结版本、澄清与重试等分支；以上真实模型链路单独验收，不混称为真实业务验证。

## 基线与当前边界

- 集成前全量后端：1560 passed、14 skipped、5 failed。4 项审批/运行配置测试已在 git archive HEAD 独立副本复现；另 1 项 shadow-code 测试命中既有 .factory-preview/release/cutover.py 与 .factory-preview/server.py。没有删除用户演示文件掩盖基线结果，不能声称全库测试全部通过。
- 本次为本地实现和验收，未提交、推送或部署。验收账号与数据均独立于生产。
- 首版新建职能体使用简短表单，后续通过维护对话定制；项目须在现有项目页建立后选择关联，尚未实现聊天自动克隆仓库或通用项目附件上传。
- 写入沿用现有管理员权限边界，适合当前个人工作台。尚未新增细粒度多人协作授权体系。
- 自定义工具授权和 Provider 不支持的高级模型参数会明确拒绝；空工具配置继承现有受控 SDK 工具，不表示完全禁用工具。
- 维护上下文采用有界历史与素材读取，尚无单独的长期摘要模块；超大素材不保证一次完整纳入模型上下文。
- 本次真实验收是文档任务，证明维护规则进入执行闭环；没有声称已完成真实工业格式转换、跨平台安装包构建或签名验证。
