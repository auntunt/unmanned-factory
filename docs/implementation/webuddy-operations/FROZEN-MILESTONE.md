# 维护任务原型冻结记录

冻结日期：2026-09-21。用户指令：冻结，提交 GitHub。

- 已验收代码：`c5dc518bd8fe12960e90e3d48777449564d51a78`
- 交付分支：`codex/operations-20260921`
- 里程碑标签：`webuddy-v1-maintenance-prototype-2026-09-21`
- 标签指向包含本记录的文档提交；产品代码与上述已验收代码完全一致。
- 本里程碑是原型冻结，不是生产发布；本次不部署、不合并集成分支。

## 冻结范围

维护任务网页创建、基线登记、服务领取、计划展示与批准、进度及阻塞原因、补充状态、
回执导出与补丁下载；Issue 维护、接口适配方法模板及三个共享方法；官方本地演练入口。

## Codex 已完成的独立验收

- `08c5857`：维护相关后端 88 passed，维护页面 24 passed，生产构建通过；
  浏览器验证创建、计划展示、批准与实际执行状态。
- `c5dc518`：维护页面 26 passed，维护 HTTP 15 passed，生产构建通过。
- 官方 `scripts/preview_v3.py` 启动真实 HTTP 服务后：创建 201 → 批准 200 →
  已交付；回执导出 200，补丁下载 200、472 字节，内容包含 `welcome.txt` diff。
- 演练使用明确标注的脚本执行器；HTTP、持久化、项目检查及 Git 补丁是真实执行。

这些是各次定向验收记录，不合并表述为一次全量通过。

## 保留边界

真实模型 coding 质量、客户业务效果、生产部署及 M5 现场验收未包含在本次冻结中。
两个新方法包保持 `template-unbenchmarked`。CI 不作为本记录中的新增验收证据。
其余后置事项沿用 `TEST-FOLLOWUP.md`，不继续扩展本轮修复。

## 恢复本里程碑

```bash
git fetch origin --tags
git switch -c restore/maintenance-prototype-20260921 webuddy-v1-maintenance-prototype-2026-09-21
```

该操作创建独立恢复分支，不重置当前开发分支；服务器回滚需要另行执行部署流程。
