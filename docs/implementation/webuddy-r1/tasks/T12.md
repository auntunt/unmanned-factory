任务 ID / 所属阶段：T12 / Codex r2 复核修复 R2-02
用户结果（一句话）：普通英文单词不再被子串误判为 CLI；用户明确的交付形态不被「不部署」类否定连带抹掉；歧义仍保守返回未声明。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t09，分支 webuddy-r2-t09（其 T09 改动已合并主线；本单追加增量）。独占本 worktree。
缺陷（Codex 已复现）：deliverables.py `infer_delivery_type_from_text`：
a) 'Build a client portal' → cli（client 含 cli 子串）。
b) goal='做一个网站' + non_goals=['部署到线上服务'] → None（负向减除把「线上服务」当类别证据，连 service 一起抹掉）——交付形态与是否部署是两回事。
修复契约：
- 英文/ASCII 关键词按词边界匹配（regex \b 或等价 token 化）；中文与 '.exe' 等扩展名保持子串匹配。
- 负向信号只对「交付形态」词生效（桌面应用/安装包/CLI/命令行等），部署/上线类措辞（部署、上线、线上服务等出现在 non_goals）不得作为类别的负向证据；正向侧同理审视：类别关键词表里兼作部署措辞的词（如「线上服务」）在 non_goals 中出现不构成对 service 的否定。设计上允许你重构关键词分组，但保持行为可测。
- 保守策略不变：唯一正向命中才定型，真歧义 None。
完成条件（这些行为各一条测试，先写红再修绿）：
1. 'Build a client portal' → None（不是 cli）；'build a cli tool' → cli。
2. goal 网站 + non_goal 部署到线上服务 → service。
3. '做个网站，不要做成桌面应用' → service（既有测试保持）。
4. non_goals 单独出现形态词不产生类型；正向歧义仍 None（既有测试保持）。
5. 经正常需求确认流程（requirement_analysis.confirm 路径）的既有 3 条测试保持通过。
验证：`uv run pytest tests/test_control_deliverables.py tests/test_requirement_analysis.py -q`。
明确不做：不引入模型调用做推导；不改 confirm 写入点；不动前端。
证据输出位置：docs/implementation/webuddy-r1/receipts/T12.md（含修复前失败输出）。commit 到本分支，不推送不合并。
