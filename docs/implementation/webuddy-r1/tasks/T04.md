任务 ID / 所属阶段：T04 / S3 团队工具复用
用户结果（一句话）：工具调用界面不再是 MFD 专属：按职能包能力声明呈现输入范围与结果，错误/取消/未知/超时分别显示；用非 MFD 的真实 CLI（内置 csv-quote-xml 包）实测证明通用接入；MFD 继续如实标「清单局部提取」。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r1-t04，分支 webuddy-r1-t04，基线 bb2cd88。独占此 worktree，禁止碰其他 webuddy-r1-* 或 v3-skills-icons。
依赖及其状态：职能包生命周期已落地（f81622a）：pack_routes.py /api/v4/capability-packs、CapabilityPanel.tsx、内置包 factory/control/builtin_packs/tools/{csv-quote-xml,mfd-xml}。只读复用其 API，不改 pack_runtime/pack_sandbox 隔离层。
页面编号与状态：B03 职能体对话（工具输入/结果/错误态）、B05 职能包详情（涉及文案时）。设计材料只读 page-contracts.md 的 B03/B05 小节、05-claude-handoff.md 的 S3 一节、SKILL references 三件。
必须复用的组件与服务：CapabilityPanel.tsx 既有上传/调用/generation 守卫逻辑；包 manifest 的工具契约（输入格式、限制来自能力声明，不得硬编码）。
允许改动范围：frontend/src/conversation/CapabilityPanel.tsx 及其测试、conversation.css 中该面板样式、pack 相关用户可见文案（含 builtin_packs 两个包的 README/manifest 描述字段如确有 MFD 专属措辞泄漏到通用 UI）、必要时 pack_routes.py 只读查询字段暴露（新增字段先报规划者）。
明确不做：不改隔离/执行层（pack_runtime/pack_sandbox）；不建工具商城；不加新顶层入口；不扩 MFD 成功口径（保持「清单局部提取」）；不做模型型 CLI 联网代理（F04）；普通成员提交材料不得创建 Coding run。
最小数据契约：输入支持范围/大小限制读包 manifest 的工具契约字段；无声明字段时显示「未声明」而非猜测；执行状态用后端真实 status（queued/running/succeeded/failed/cancelled + isolation_unavailable 等失败原因），错误、取消、未知、超时四类分别渲染，不合并成一个红字。
完成条件（可观察行为）：
1. 真实进程实测：临时库起服务，用 csv-quote-xml 包走「候选→验证→发布→挂靠→上传 fixtures/sample_quote.csv→调用→下载结果」，回执附事件流与产物摘要（这是 V1-07 的非 MFD 实证；后端 API 已支持，重点是界面按声明呈现）。
2. 组件测试覆盖：能力声明渲染（含未声明字段）、四类失败态、取消态、刷新后从任务历史恢复结果。
3. MFD 包在同一界面下显示其真实支持范围与「局部提取」定位，无业务值伪装。
必要验证：前端 `cd frontend && npm install && npm test -- src/conversation/CapabilityPanel.test.tsx` + 受影响测试；后端只在改了 pack_routes 时跑 `uv run pytest tests/test_pack_api_durability.py tests/test_capability_packs.py -q`。记录命令与退出码，不跑全量。
证据输出位置：docs/implementation/webuddy-r1/receipts/T04.md；commit 到本分支，不推送、不合并。
变更或阻塞报给谁：规划者。
