任务 ID / 所属阶段：T06 / S1（S03 公司测试环境配置区）
用户结果（一句话）：用户在 /settings/runtime 的配置区看到公司测试环境（部署目标）的真实状态：已配置目标列表、连通性检查结果；缺配置时显示明确的设置入口与所缺项，而不是假就绪或隐藏。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r1-t06，分支 webuddy-r1-t06，基线 bb2cd88。独占此 worktree，禁止碰其他 webuddy-r1-* 或 v3-skills-icons。
依赖及其状态：后端 deploy_targets.py（TargetStore：list/get/create/update/bind/snapshot、密钥指纹）与 remote_targets.py（test 连通检查）、remote_routes.py 已存在。先调查这些 API 的真实形状再动 UI。
页面编号与状态：S03 公司测试环境（scope-lock 明确：优先做成 /settings/runtime 内配置区，不新建路由/一级入口）。设计材料只读 page-contracts.md 的 S03 小节、scope-lock.md 第 2/4 节、SKILL references 三件。
必须复用的组件与服务：现有 SettingsPage.tsx（或 /settings/runtime 实际所在组件，先用代码确认）、AppShell、既有表单与徽标样式。
允许改动范围：frontend/src/conversation/SettingsPage.tsx（或实际承载 runtime 设置的组件）及其测试、必要的小型 api 封装（frontend/src/lib 内）；后端仅当缺只读查询时在 remote_routes.py 补最小 GET（新增写接口先报规划者）。
明确不做：不做蓝绿/多域名（固定公司测试地址是产品决定）；不在页面输入或显示私钥/凭据明文（指纹与名称可显示）；不做真实部署动作（Codex）；不新建导航；不动 F01～F06。
最小数据契约：目标状态只用后端真实字段（连通检查结果/时间）；没有检查记录显示「未检查」，不显示假在线；检查触发用既有 test 接口。
完成条件（可观察行为）：
1. 无目标时：配置区显示「尚未配置公司测试环境」+ 指向创建表单/入口 + 所需字段说明；不显示假就绪。
2. 有目标时：列表显示名称、地址、密钥指纹、最近连通检查结果与时间；可手动触发一次连通检查并显示真实结果（成功/失败原因）。
3. 组件测试覆盖：空态、有目标态、检查失败显示、刷新恢复；桌面与手机断点不遮挡输入。
4. 真实进程验证：临时库起服务，注册一个指向本机的目标（如 ssh localhost 或 fake 但如实标注），页面真实调 API 截图或 DOM 断言。fake 与真实分别标注。
必要验证：前端 `cd frontend && npm install && npm test -- <SettingsPage 相关测试>` + `npx tsc --noEmit`；后端如有改动跑 `uv run pytest tests/test_remote_targets.py -q`。记录命令与退出码，不跑全量。
证据输出位置：docs/implementation/webuddy-r1/receipts/T06.md；commit 到本分支，不推送、不合并。
变更或阻塞报给谁：规划者。
