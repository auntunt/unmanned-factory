任务 ID / 所属阶段：T02 / S1 软件交付主线
用户结果（一句话）：一句真实小需求在本机穿过 planning→execution→verifying→成果落库，再用 follow-up 完成第二轮修改且保留第一轮成果——V1-01/02/04 的本地段拿到真实证据；发现的真实缺陷小范围修复。
已核对基线（工作区 / 分支 / HEAD / 未提交变化）：/Users/auntlee/workspace/.factory-worktrees/v3-skills-icons，分支 v3-conversation-workspace，HEAD 见执行时 `git log --oneline -1`（T01 后为 docs-only 提交），开工前 `git status` 必须干净或仅含本批次 docs。
依赖及其状态：T01 已 local_reviewed；断点清单与事实见 docs/implementation/webuddy-r1/receipts/T01.md（必读，尤其「下一任务需要知道的事实」）。
页面编号与状态：W01 开始制作 / W02 开发任务（执行中→完成→继续修改）。本单以 API+真实执行为主，前端仅在缺陷涉及时小改。
只读这些设计材料：
- webuddy-delivery/SKILL.md 执行者职责 + references/{executor.md,verification-policy.md,task-contract.md}
- webuddy-v1/01-scope-and-acceptance.md 的 V1-01/02/04 三行
必须复用的组件与服务：现有 SDKRunner/providers、run_lifecycle、run_routes、RuntimeSettings 配置路径（FACTORY_*_PROVIDER/MODEL）。不新建执行器、不换模型路由策略。
允许改动范围：
1. 先诊断 T01 的 B1：宿主 claude CLI 已登录，探针子进程报 Not logged in——查 SDKRunner 派生子进程的 env/HOME/PATH 传递与 provider 选择（默认 codex 但本机可用的是 claude），用真实配置（如 FACTORY_*_PROVIDER=claude）跑通，不输入任何凭据、不把 token 写进代码或提交。
2. 若确属代码缺陷（如环境变量剥离、provider 选择错误提示不清），在 factory/control/{providers,runtime,sdk_worker,run_execution}.py 相应处最小修复并补一条能复现该缺陷的定向测试。
3. 真实链路证据：临时 FACTORY_CONTROL_DATA 起服务，用一个真实小需求（建议：单文件 Flask/FastAPI 计数器小应用之类，几分钟能完成的）创建 run，等待真实执行完成；再 follow-up 一条真实修改要求，验证第二轮完成且 history/artifacts 保留第一轮。
4. GitHub 段：若本机已有可用 GitHub 授权（先查现有配置，不新建凭据），对一个测试仓库真实走一次 bind→publish 并记录回执；没有则记录确切缺口留给 Codex。
明确不做：不部署服务器（Codex）；不改 follow-up 的 ACTIVE 待应用机制（T03）；不动 F01～F06；不新增入口/按钮；不把 stub 测试改成断言真实网络。
最小数据契约：使用现有字段；执行证据 = run 的真实 status 流转事件 + artifacts + 落盘产物路径。
完成条件（可观察行为）：
1. 一个 run 从 received 真实到 succeeded/verified（贴事件流与 artifacts 摘要），产物代码在工作区可查看且能本地运行其自测。
2. 同一 run follow-up 后第二轮真实完成，第一轮成果与 history 保留，能辨认两轮各自产物。
3. B1 的根因写清：环境问题（写明确切前置条件与配置命令）或代码缺陷（附修复 diff+复现测试）。
4. GitHub 段：真实回执或确切缺口说明，二选一，不得含糊。
必要验证：定向 `uv run pytest tests/<受影响文件> -q`；真实执行的成本与耗时记入回执。已通过且未受影响的套件不重跑。
证据输出位置：docs/implementation/webuddy-r1/receipts/T02.md
变更或阻塞报给谁：规划者。同一阻塞两轮定向尝试无进展即停，记录复现材料。
