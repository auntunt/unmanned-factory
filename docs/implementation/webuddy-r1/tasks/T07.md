任务 ID / 所属阶段：T07(A) / 验收前修复——平台自动验证命令
用户结果（一句话）：真实任务的「开发→平台自动验证」不再因 checks 命令 Errno 2 停在 needs_human：合法命令真实执行，非法格式得到可理解错误，counter 示例经平台流程复验通过验证段。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t07，分支 webuddy-r2-t07，基线 5018e7d。独占本 worktree。
依赖及其状态：无前置。已知事实：执行端 factory/control/execution.py `_run_check_unlimited` 以 argv 列表 Popen（模块注释「argv based subprocesses」）；T02 探针写入 checks 为 `{"test": ["python3 -m pytest test_counter.py -v"]}`——单元素含空格字符串被当作程序路径 exec → [Errno 2]。T02 回执见 docs/implementation/webuddy-r1/receipts/T02.md。
只读设计材料：SKILL references 三件（executor/verification-policy/task-contract）；05-claude-handoff.md S1 一节。
调查要求（先做）：查清 checks 的完整链：谁生成（checkgen/规划/用户 API）、存储 schema（project['checks'] 的形状与校验点）、执行端消费。判断错误层：生成端产出坏格式？存储端未校验？执行端应兼容？把结论写进回执再动手。
允许改动范围：execution.py 的 checks 执行/校验、checks 存储入口的格式校验（project_routes/planning 等实际所在处，调查后确定并在回执列明）、对应测试。不改 run_execution.py / run_lifecycle.py（T08 在改，确需先报规划者）。
契约③（修复须满足）：
- 表示：argv 列表（程序名+参数逐元素）为规范格式；工作目录沿用现有 root 语义。
- 兼容：既有合法 argv 列表不变；对「单元素含空格字符串」这类历史/手输形态，在明确定义的一层做确定性处理（如 shlex.split 或入库时规范化——由调查结论决定），不得整体改 shell=True。
- 非法格式（空、非字符串元素、不可解析）返回结构化、用户可理解的错误进入 check 记录，不抛裸异常卡任务。
- 成功/失败/超时/取消结果真实进入任务状态与事件（现有 check.result 路径）。
完成条件（可观察行为）：
1. 单元测试覆盖：规范 argv 执行成功；含空格单字符串按新契约处理；非法格式得到结构化错误；超时与取消路径不回归（沿用既有用例）。真实子进程验证，不 stub Popen。
2. 平台流程复验：临时库起服务，用真实模型跑 counter 类小任务（参照 T02 回执方法），checks 配置走正常入口写入，任务穿过验证段（check.result 事件含真实 exit code），不再 Errno 2。手工跑 pytest 不算数；记录真实成本。
3. 若调查发现生成端（如 checkgen）也产坏格式，修生成端并附回归；生成端不在本仓库范围则如实记录缺口。
必要验证：`uv run pytest tests/<execution/checks 相关> -q` 定向；不跑全量。记录命令/退出码/日志路径；真实模型运行记录 run_id 与事件流摘要。
证据输出位置：docs/implementation/webuddy-r1/receipts/T07.md；commit 到本分支，不推送不合并。
变更或阻塞报给谁：规划者。
