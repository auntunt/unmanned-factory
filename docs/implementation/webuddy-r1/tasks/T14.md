任务 ID / 所属阶段：T14 / Codex 服务器复核修复 S2（进度笔记与范围契约）
用户结果（一句话）：平台自己要求写的 .webuddy/coding-progress.md 不再成为唯一 undeclared 而判 fail；其余 .webuddy 与业务文件仍受范围管控。
基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t14，分支 webuddy-r2-t14 @ 0b39155。独占。
缺陷（服务器实测）：continuous.py _INSTRUCTIONS 要求写 .webuddy/coding-progress.md；scope_declaration.py exempt 只含 .spec/ 与测试文件。真实证据：actual=[.webuddy/coding-progress.md, counter.py, test_counter.py]，declared=[counter.py, test_counter.py]，undeclared=[.webuddy/coding-progress.md]，status=fail。
修复契约（自行审查最小合理方案，二选一并写明理由）：
- 方案 A：仅该固定笔记路径的窄豁免（精确路径 '.webuddy/coding-progress.md'，不是 .webuddy/ 目录）；必须处理符号链接边界——该路径若是符号链接不得豁免；其余 .webuddy 下文件仍按未声明处理。
- 方案 B：平台管理/提前自动声明该路径（在声明侧固定加入）。
- 禁止：豁免整个 .webuddy 目录、任何业务源码；关闭或绕过 scope reconciliation；改库假造声明。旧现场证据不改（Codex 要求修复后另记新结果）。
完成条件（先红后绿）：1) 复现服务器场景的回归（上述 actual/declared）修复前 fail、修复后 pass 且 exempt/归属如实入 scope 记录；2) '.webuddy/evil.py' 未声明 → 仍 fail；3) coding-progress.md 为符号链接 → 不豁免（如选方案 A）；4) 既有 exempt 用例保持。
验证：grep 找 scope_declaration/continuous 相关测试，定向 `uv run pytest tests/<相关> -q`。不跑全量/前端。
证据输出：docs/implementation/webuddy-r1/receipts/T14.md（含修复前失败输出）。commit 本分支，不推送不合并。
