任务 ID / 所属阶段：T13 / Codex 服务器复核修复 S1（验收响应解析）
用户结果（一句话）：真实验收回复「说明段 + 唯一 JSON 代码围栏」能被可靠解析并继续走全部既有验收规则；歧义与非法形式仍被拒绝。
基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t13，分支 webuddy-r2-t13 @ 0b39155。独占。
缺陷（服务器实测）：verification.py 约 286 行只接受纯 JSON 或全文围栏；真实回复为一段英文说明后接唯一 ```json 围栏（verdict=pass、reason、acceptance_coverage 全表、operation_results={}），被判 invalid_response。原文样例见 /Users/auntlee/Desktop/自动化harness构建/docs/webuddy-r2-server-blockers-2026-09-18.md S1 节（verification-response.txt 稍后到位，先按引文构造）。
修复契约：
- 接受三种形式：纯 JSON；全文围栏；说明文本 + **唯一** 明确 JSON 代码块（```json 或 ``` 内为单个合法对象）。
- 必须拒绝：多个候选 JSON 块、冲突 verdict、无合法对象、非法字段（verdict∉{pass,fail,unverified} 或 reason 非字符串）→ 维持 invalid_response 路径。
- 禁止：搜索任意 pass 字样、regex 跨对象拼接、忽略 coverage 放行。解析成功后 coverage/fidelity/scope/browser 等全部既有规则原样执行——模型说 pass ≠ 平台 pass。
- 若仓库已有围栏/JSON 提取函数可复用（先 grep），优先复用并写明。
完成条件（先红后绿）：1) 复现真实形式的回归（说明段+唯一 json 围栏）修复前 invalid_response、修复后正常进入 coverage；2) 两个 JSON 块/冲突 verdict → 仍 invalid_response；3) 既有纯 JSON 与全文围栏用例保持；4) 解析后规则不被短路的断言（如 coverage 不完整仍按既有逻辑处理）。
验证：先 grep 找 verification 相关测试文件，定向 `uv run pytest tests/<相关> -q`。不跑全量/前端。
证据输出：docs/implementation/webuddy-r1/receipts/T13.md（含修复前失败输出）。commit 本分支，不推送不合并。
