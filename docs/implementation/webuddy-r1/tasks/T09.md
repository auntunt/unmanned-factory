任务 ID / 所属阶段：T09(C) / 验收前修复——交付类型数据来源与页面一致
用户结果（一句话）：delivery_type/installer_targets 由正常用户流程产生、保存并在刷新/重试/继续修改后保持一致；RunWorkspace 与 Deliverables.tsx 展示规则一致；旧任务未声明不伪造。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t09，分支 webuddy-r2-t09，基线 5018e7d。独占本 worktree。
依赖及其状态：T05 已合并：deliverables listing 只读输出 delivery_type/installer_targets（显式 artifacts 字段 > installer 物件推断 > null）；结果卡 DeliverySection 三态渲染。缺口（Codex 初审确认）：真实写入流程未接。先读 receipts/T05.md 与 intake 文档交付类型两行。
只读设计材料：SKILL references 三件；scope-lock.md 第 2/4/6 节；page-contracts.md W01/W02/W03 相关小节；05-claude-handoff.md S4 一节。
契约⑤（调查后落实，重大偏差先报）：
- 来源顺序：需求文本/需求分析已能确定类型（如「做个网站/SaaS」→service、「命令行工具」→cli、「安装包」→installer）时直接写入 run.artifacts.delivery_type，写入点放在现有需求分析/规划产物落库处（调查 requirement_analysis / planning / run 创建链后确定；确需改 run_execution.py 或 run_lifecycle.py 先报规划者——T08 正在改）。只有决定性歧义才走既有澄清机制，不新增澄清页面。
- 持久与一致：类型随 run.artifacts 持久，刷新/重试/continue 不变（继续修改沿用原类型除非用户明确改）。
- installer_targets 只在用户明确给出目标系统时记录；平台无该系统构建能力时如实显示缺口文案，不建打包系统。
- 展示一致：RunWorkspace 与 Deliverables.tsx 用同一份标签/规则（可共享常量），旧任务 null → 「未声明」。
允许改动范围：delivery type 推导/写入的后端小改（deliverables.py + 调查确定的写入点）、frontend/src/workbench/Deliverables.tsx、RunWorkspace.tsx 仅结果卡/DeliverySection 区（不碰消息/徽标区，T08 在改）、共享常量文件、对应测试。
完成条件（可观察行为）：
1. 从正常创建流程进入的测试：以「CLI 工具」措辞创建的 run，其 deliverables listing 返回 delivery_type=cli；service 同理；措辞含混→null。禁止直接手工塞库字段当证据（夹具搭建除外，断言必须走真实推导/写入路径）。
2. 刷新/重试/继续修改后类型一致的回归。
3. Deliverables.tsx 与 RunWorkspace 展示一致性测试（同一常量）；旧任务 null 显「未声明」。
4. tsc、build、相关 vitest、定向 pytest 通过。
明确不做：不加新页面/流程/一级入口；不做全平台打包；不动 F01～F06；超出既有设计的缺口在回执提出，继续不受影响部分。
必要验证：定向前后端测试，不跑全量。
证据输出位置：docs/implementation/webuddy-r1/receipts/T09.md；commit 到本分支，不推送不合并。
变更或阻塞报给谁：规划者。
