任务 ID / 所属阶段：T10(D) / 验收前修复——setting_sources 配置边界核验
用户结果（一句话）：搞清 79b7035 的 ["user"] 到底加载了什么：认证来源与用户设置（hooks/权限/env）分开核实；若有非预期加载，改为显式最小配置传递；给出服务器部署身份下的检查步骤。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t10，分支 webuddy-r2-t10，基线 5018e7d。独占本 worktree。
依赖及其状态：79b7035 已合并（setting_sources []→["user"] + CLAUDE_CODE_* 剥离）。先读 receipts/T02.md 与 intake 文档 setting_sources 行。不预设该修改正确或错误。
调查要求（先做，写进回执）：
1. 依据本机实际安装的 claude_agent_sdk 与捆绑 CLI（查版本、读其文档/行为，不猜）：--setting-sources 各取值下，认证凭据（keychain/.credentials.json/ANTHROPIC_* env）分别是否可用？"user" 还会加载哪些：~/.claude/settings.json 的 hooks？permissions？env？CLAUDE.md？
2. strict_mcp_config 实际只覆盖什么（MCP servers？plugins？），列出它不覆盖的面。
3. 在隔离配置中复现（HOME/CLAUDE_CONFIG_DIR 指向临时目录构造受控 settings.json，含无害 canary hook/permission），观察 worker 是否加载——不改用户真实配置、不输出任何凭据值、不放宽 worker 权限。
允许改动范围：providers.py（若结论要求更显式/更小的配置传递，如仅认证所需的最小 sources/env 显式传入 + 保留既有 can_use_tool/hooks 权限门）、对应测试（tests/test_control_providers.py / test_skill_ingestion.py 相关断言随行为如实更新）、回执。若结论是 ["user"] 正确且无非预期加载，不改代码，测试补一条固定该结论的回归（如 canary hook 不生效的断言，做得到的话）。
完成条件（可观察行为）：
1. 回执给出明确矩阵：设置源取值 ×（认证可用？hooks 加载？permissions 加载？env 注入？MCP 加载？），每格注明证据来源（隔离实验/文档/源码）。
2. 隔离实验脚本与输出摘要（无凭据值）。
3. 若改了 providers：真实模型 smoke 一次（短请求）证明认证仍通 + 定向测试；若未改：结论回归测试。
4. 「服务器部署身份下的检查步骤」清单（Codex 执行）：查什么文件、看什么字段、预期什么。
必要验证：定向 pytest；真实模型 smoke（如适用）记录成本。不跑全量。
证据输出位置：docs/implementation/webuddy-r1/receipts/T10.md；commit 到本分支，不推送不合并。
变更或阻塞报给谁：规划者。
