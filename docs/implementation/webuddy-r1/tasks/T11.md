任务 ID / 所属阶段：T11 / Codex r2 复核修复 R2-01
用户结果（一句话）：含空格的合法单元素可执行路径不再被拆坏，同时保留 T07 的命令字符串兼容与结构化报错；不用 shell=True 兜底。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r2-t07，分支 webuddy-r2-t07（其 T07 改动已合并主线；本单在此分支追加增量）。独占本 worktree。
缺陷（Codex 已复现）：execution.py `_normalize_check_argv` 对单元素含空格无条件 shlex.split；`["/tmp/My Tools/checker"]` 这类合法 argv 被拆成两元素 → FileNotFoundError errno 2。
修复契约：确定性区分两种形式，禁止 shell=True：
- 单元素含空格时，若该字符串本身指向存在的可执行文件（绝对路径，或相对检查工作目录 root 解析后存在），按原生 argv[0] 原样保留；否则按命令字符串 shlex.split。注意 _check_argv 当前拿不到 root——调查调用链后把存在性判定放在能拿到真实检查 cwd 的位置（或把 root 传入），写明理由。
- 既有行为全保留：多元素 argv 不动；`["python3 -m pytest ..."]` 仍拆分；非法格式仍结构化报错；NUL 检查不动。
完成条件：
1. 先写 Codex 复现的回归（临时目录建 `My Tools/checker` 可执行脚本，argv=[该绝对路径]，真实子进程执行 exit 0）——修复前失败，修复后通过；再补相对路径含空格场景（如适用）。
2. T07 既有 13 条测试与真实子进程用例全部保持通过（拆分、结构化错误、失败退出码）。
3. `uv run pytest tests/test_control_execution.py -q` 通过。
明确不做：不改 run_execution/run_lifecycle；不放宽任何校验。
证据输出位置：docs/implementation/webuddy-r1/receipts/T11.md（含修复前失败输出）。commit 到本分支，不推送不合并。
