任务 ID / 所属阶段：T03 / S2 人的提示词干预闭环
用户结果（一句话）：任务运行中提交的补充不丢、不要求重输：显示已记录→待应用→已应用，服务端在安全节点按序消费，重试幂等、刷新与重启后仍在。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r1-t03，分支 webuddy-r1-t03，基线 bb2cd88（f81622a + docs）。独占此 worktree，禁止碰其他 webuddy-r1-* 或 v3-skills-icons。
依赖及其状态：无前置。已知缺口：run_routes.py follow_up 中 status in ACTIVE 分支只存 user.message(applied=false) 并回复「任务暂停或完成后，请再次提交这些要求」——这正是要修的。
页面编号与状态：W02 开发任务（执行中补充/待应用/已应用/取消中）。设计材料只读 page-contracts.md 的 W02 小节与 05-claude-handoff.md 的 S2 一节；SKILL 的 references/{executor.md,verification-policy.md,task-contract.md}。
必须复用的组件与服务：既有事件存储（store._event/export_events）、svc.lock、continue_run/clarify/cancel、RunWorkspace.tsx 既有 composer 与消息渲染。不建新表、不建调度框架。
接口契约（规划者已定，变更先报规划者）：
- ACTIVE 期提交：持久事件 `followup.pending` {id, content, actor_id, actor, fingerprint, created_at}；响应 {recorded:true, queued:true, applied:false} + 面向用户的说明文案（不再要求重输）。
- 安全节点消费：run 进入 needs_human / needs_clarification / awaiting_approval 或终态后的下一轮启动时，服务端按提交顺序把未消费 pending 并入该次 continue/clarify 的内容，并记事件 `followup.applied` {pending_id, run_revision}；同一 pending 不得双消费（重试/重启下）。
- 幂等：沿用 idempotency_key 回放（同 key 异内容 409 语义保持）。
- 取消：cancel_requested/取消完成不清除未消费 pending，界面区分「取消中」与「已取消」，pending 显示保留。
- 重启恢复：pending/applied 均从事件重建，不新增表；服务重启后未消费项仍待应用。
允许改动范围：factory/control/run_routes.py（follow_up 及必要查询）、run_lifecycle.py（安全点消费）、service.py 中启动消费必需的最小接线、frontend/src/conversation/RunWorkspace.tsx 与 run-state.ts（状态徽标：已记录/待应用/已应用/取消中）、对应测试文件。
明确不做：不承诺即时打断执行器；不改 providers/runtime/sdk_worker/run_execution（T02 负责）；不动 SettingsPage/CapabilityPanel（他人负责）；不新增入口按钮；不动 F01～F06。
最小数据契约：见上接口契约；不支持在线追加的执行器场景按「安全节点衔接」处理并如实显示。
完成条件（可观察行为）：S2 要求的场景各有真实状态转换测试：活跃任务补充、重复请求（同 key 重试）、收到后服务重启（新建 Service 实例重放）、用户刷新（GET 恢复显示）、冲突要求（同 key 异内容 409）、取消中提交、消费后不重复消费。前端 vitest 覆盖徽标三态与刷新恢复。不只断言回执字符串。
必要验证：后端 `uv run pytest tests/test_run_followup.py <新增/受影响文件> -q`；前端 `cd frontend && npm install && npm test -- src/conversation/RunWorkspace.test.tsx`（如 node_modules 缺失先装，不改锁文件）；记录命令与退出码。不跑全量（集成时规划者统一跑）。
证据输出位置：docs/implementation/webuddy-r1/receipts/T03.md；改动按逻辑分次 commit 到本分支，不推送、不合并。
变更或阻塞报给谁：规划者。
