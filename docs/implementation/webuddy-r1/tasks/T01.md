任务 ID / 所属阶段：T01 / S1 软件交付主线（探针）
用户结果（一句话）：确认"一句需求→开发→测试→GitHub→固定公司测试地址→同任务修改"这条链在当前代码里到哪一步断，产出可复现的断点清单，供 T02 起逐个修复。
已核对基线：/Users/auntlee/workspace/.factory-worktrees/v3-skills-icons，分支 v3-conversation-workspace，HEAD f81622a，干净。
依赖及其状态：无（S0 已收尾）。
页面编号与状态：W01 开始制作 / W02 开发任务（执行中→完成→继续修改）/ S03 公司测试环境。只核对现状，不改页面。
只读这些设计材料：
- /Users/auntlee/Desktop/自动化harness构建/docs/design/webuddy-uiux-v1/webuddy-delivery/SKILL.md（执行者职责：references/executor.md）
- /Users/auntlee/Desktop/自动化harness构建/docs/design/webuddy-v1/01-scope-and-acceptance.md 的 V1-01～V1-04 四行
- /Users/auntlee/Desktop/自动化harness构建/docs/design/webuddy-v1/05-claude-handoff.md 的 S1 一节
必须复用的组件与服务：factory/control/{run_lifecycle,run_routes,github_publication,deploy_targets,remote_targets,routing}.py、frontend/src/conversation/{StartChat,RunWorkspace}.tsx。只调查与调用，不重写。
允许改动范围：仅 docs/implementation/webuddy-r1/receipts/T01.md 与该目录下的证据文件。产品代码零改动；若发现必须先修才能继续探针的缺陷，记录并停在该断点，不顺手修。
明确不做：不跑真实模型执行器烧钱跑完整开发任务；不部署；不改测试；不动 F01～F06；不评审 UI 视觉。
最小数据契约：使用现有 API 与存储字段；不新增字段。
完成条件（可观察行为）：
1. 按 V1-01→V1-04 顺序给出每一步的现状判定：已接通（引用真实代码路径+已有测试名）/ 断点（可复现步骤+实际输出）/ 需环境（具体缺什么）。
2. 至少用真实进程验证到"创建任务→计划/需求分析→状态持久化→刷新恢复"这段：启动控制服务（uv run 或仓库文档写明的方式，SQLite 临时库），用 curl/httpx 走真实 API 创建一个小任务；执行器如需真实模型凭据/成本，记录到此为止的确切阻塞，不 mock 冒充跑通。
3. GitHub 发布与部署目标两段：以已有测试套件（如 tests/ 下 github/deploy/remote 相关用例）的定向运行结果 + 代码走读为证据，明确哪些只被 stub 测试覆盖、缺真实端证据。
4. 断点按用户遇到的顺序排列，标注每个断点建议的最小修复范围（给 T02 用，不实现）。
必要验证：工作目录 /Users/auntlee/workspace/.factory-worktrees/v3-skills-icons；后端定向 `uv run pytest tests/<相关文件> -q`（先用 ls/grep 找真实文件名，不猜）；记录每条命令、退出码、日志位置。已在 f81622a 通过的套件不重跑全量。
证据输出位置：docs/implementation/webuddy-r1/receipts/T01.md（按 task-contract.md 执行回执格式）。
变更或阻塞报给谁：规划者（回执中写明）。
