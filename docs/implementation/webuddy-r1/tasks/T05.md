任务 ID / 所属阶段：T05 / S4 界面收口与交付类型
用户结果（一句话）：任务结果卡能区分交付类型（服务/安装包/CLI），不适用的部署步骤不显示为失败；安装包未选定目标系统时如实显示待确认，不编造支持平台；W02 结果区在桌面与手机上无遮挡。
已核对基线：工作区 /Users/auntlee/workspace/.factory-worktrees/webuddy-r1-t05，分支 webuddy-r1-t05，基线 b697494（已含 T02/T03/T04/T06 集成与修复）。独占此 worktree。
依赖及其状态：T03 已合并（RunWorkspace 含 followups 徽标，Run 类型有 followups 字段）；T02 已合并（真实执行链可用）。先 git log 核对基线再动手。
页面编号与状态：W02 开发任务（结果卡）、W03 历史作品（交付类型标识如涉及）。设计材料只读 page-contracts.md 的 W02/W03 小节、scope-lock.md 第 2/6 节（含安装包例外条款）、05-claude-handoff.md 的 S4 一节、SKILL references 三件。
必须复用的组件与服务：现有 deliverables.py / run artifacts 元数据、RunWorkspace 结果区、GithubDelivery/Deliverables 组件、既有图标（workbench/Icon.tsx）。先调查后端已有交付元数据字段，优先复用，不建通用工作流引擎。
允许改动范围：factory/control/deliverables.py 小范围扩展（如需 delivery_type 字段，先查现有字段能否表达；新增字段属最小增量并在回执报规划者）、frontend/src/conversation/RunWorkspace.tsx 结果卡区域、frontend/src/workbench/Deliverables.tsx、相关测试与 CSS。
明确不做：不做安装包构建/多 OS 支持（未选定目标系统，显示待确认）；不做蓝绿/多域名；不新增一级入口或任务类型；不动 F01～F06；不改 T03 刚落地的 followup 徽标逻辑；不把源码 ZIP 标成安装包。
最小数据契约：delivery type 优先从既有 run/project 元数据推导；推导不出时显示「未声明」，不猜测；不适用步骤（如 CLI 无部署）呈现为「不适用」而非失败红。
完成条件（可观察行为）：
1. 三种交付类型的结果卡渲染各有测试：service（含部署段）、cli（部署段不适用不显失败）、installer（目标系统未选定→待确认文案）。
2. 无法推导类型的旧任务显示兼容（未声明态），不误标。
3. 桌面+手机断点（vitest+jsdom 断言布局类名/可见性即可）无输入遮挡回归。
4. tsc --noEmit、npm run build、相关 vitest 通过；改后端则相关 pytest 通过。
必要验证：`cd frontend && npx vitest run src/conversation/RunWorkspace.test.tsx src/workbench/<相关> && npx tsc --noEmit && npm run build`；后端定向 pytest。记录命令与退出码，不跑全量。
证据输出位置：docs/implementation/webuddy-r1/receipts/T05.md；commit 到本分支，不推送、不合并。
变更或阻塞报给谁：规划者。
