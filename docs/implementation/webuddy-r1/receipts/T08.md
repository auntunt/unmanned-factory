任务 ID / 状态：T08 / implementation_reported
实际执行模型（能核对则填；否则注明无法核对）：无法核对（SDK 不报告模型 ID）
实际基线 → 当前提交或 diff：f4d7cb8 → e6660be（三次提交：1e1e925 实现 + 42cb872 回执 + e6660be 监工修正）
用户行为发生了什么变化：
  - 运行中提交补充后，runner 到达 needs_human 时系统自动以合并内容接续执行，全程无第二次用户 API 调用。
  - 事件流含 run.auto_resumed，payload 记 actor=system/auto、pending_ids 和合并后的 answer。
  - followup.applied 在 auto_resume 的 store.update 成功后才写（沿用 T03 的 collect→校验→update→mark 序）。
  - 正常完成（ready_for_review）后，未消费 pending 标为 followup.expired（任务已结束未并入），不自动开新一轮。
  - 取消状态保留 pending 原状，不自动恢复已取消任务。
  - 前端消息徽标按各消息自身 pending_id 查对应 followup 条目，两条补充一条并入一条仍 pending 时逐条如实显示。
  - 三态措辞：待应用（pending 橙色）/ 已并入后续执行（applied 绿色）/ 任务已结束未并入（expired 灰色）。
  - store.conversation() 现传递 pending_id 到前端，实现真正的逐条回执。
  - 刷新/重启后以上状态从事件重建；并发提交方向为重复而非丢失。

改动文件与范围偏差：
  - factory/control/run_lifecycle.py — 新增 _expire_unconsumed_followups、_auto_resume_with_followups
  - factory/control/run_execution.py — _run 异常处理中调 _auto_resume_with_followups、成功路径调 _expire_unconsumed_followups
  - factory/control/run_routes.py — _followup_status 增加 followup.expired 事件读取
  - factory/control/store.py — conversation() 传递 pending_id
  - frontend/src/conversation/run-state.ts — followUpBadge 改为 per-item pending_id 查找、新增 expired 状态
  - frontend/src/conversation/conversation.css — 新增 .cv-followup-badge.is-expired 样式
  - frontend/src/workspace/types.ts — Run.followups 增加 expired 字段、ConversationMessage 增加 pending_id 字段
  - frontend/src/conversation/run-state.test.ts — 新增 4 条 per-item 和 expired badge 测试
  - frontend/src/conversation/RunWorkspace.test.tsx — 标签从「已应用」改为「已并入后续执行」
  - tests/test_auto_consume.py — 新增 11 个后端场景测试
  - 范围偏差：无。未改 execution.py 的 checks 逻辑，未改结果卡/DeliverySection 区。

检查（命令 / 环境 / 退出码 / 日志路径）：
  1. uv run pytest tests/test_auto_consume.py -q → 11 passed, 退出码 0
  2. uv run pytest tests/test_run_followup.py -q → 18 passed, 退出码 0（回归）
  3. cd frontend && npx vitest run src/conversation/run-state.test.ts → 15 passed, 退出码 0
  4. cd frontend && npx vitest run src/conversation/RunWorkspace.test.tsx → 18 passed, 退出码 0

自动消费触发点代码位置：
  - factory/control/run_execution.py `_run()` 函数末尾的 except Conflict/except Exception 块中，在 `self._fail(rid, exc)` 之后调用 `_auto_resume_with_followups(self, rid)`
  - factory/control/run_lifecycle.py `_auto_resume_with_followups()` 函数（约第 80 行起）

各场景测试名（tests/test_auto_consume.py）：
  1. test_auto_resume_fires_without_user_api_call — 失败暂停→自动接续
  2. test_auto_resume_event_contains_merged_answer — 事件含合并 answer
  3. test_completed_run_expires_unconsumed_pending — 正常完成→expired 态
  4. test_completed_run_does_not_expire_already_applied — 已 applied 不被 expired
  5. test_cancel_does_not_auto_resume — 取消中提交→保留不恢复
  6. test_partial_application_per_item_status — 两条补充一条并入一条仍 pending
  7. test_restart_preserves_pending_for_next_safe_node — 重启后待消费仍自动消费
  8. test_concurrent_submit_and_consume_direction_is_duplicate_not_loss — 并发/崩溃方向
  9. test_no_pending_means_no_auto_resume — 无 pending 不触发
  10. test_conversation_includes_pending_id_per_message — 逐条 pending_id 传递
  11. test_auto_resume_skipped_without_resumable_artifacts — 无可恢复现场静默跳过

前端 vitest 覆盖（run-state.test.ts 新增）：
  - followUpBadge uses per-item pending_id to determine badge
  - followUpBadge shows expired for a specific item that expired
  - followUpBadge falls back to global list when pending_id is missing
  - followUpBadge expired fallback when all are expired or applied

实际点击与截图（真实服务或测试夹具）：
  - 所有后端测试使用 app_env fixture（真实 SQLite + FakeSDK + TestClient），非 mock；状态转换经真实 store.update/export_events 验证。FakeSDK 标注为 fake runner。
  - 前端测试使用 vitest + @testing-library/react + jsdom，mock API 层验证渲染逻辑。

仍未验证的能力：
  - 真实模型执行下的 worker 跑到 needs_human 后 _auto_resume_with_followups 实际触发（需要真实 SDK runner 或集成环境中让 _run 自然失败并走到 _fail 路径）。
  - 真实并发高负载下的竞态（测试仅验证单线程序列化行为）。
  - 前端真实浏览器渲染 expired 徽标的视觉效果。

真实长任务验收步骤清单（供集成后真实模型联测与 Codex）：
  1. 启动服务，创建项目和一个真实模型任务（非 FakeSDK）
  2. 在任务 running 阶段，通过 follow-up API 提交一条补充
  3. 让 worker 正常失败或超时（使 _run 走到 _fail → needs_human）
  4. 验证：无需任何用户操作，任务应自动从 needs_human → queued → running
  5. 查看事件流：应有 run.auto_resumed 事件，payload 含 pending_ids
  6. 查看 followups 列表：补充应标为 applied
  7. 让第二轮执行正常完成到 ready_for_review
  8. 如果第二轮执行前又提了新补充但来不及消费，验证其变为 expired
  9. 取消场景：running 阶段提交补充后取消，验证 pending 保留且无 auto-resume
  10. 前端验证：刷新页面，确认各消息徽标按其 pending_id 独立显示正确三态
  11. 并发场景：快速连续提交两条补充，验证两条均被消费或按序处理

下一任务需要知道的事实：
  - _auto_resume_with_followups 在 _fail 之后同步调用，在同一个 _job 线程内完成。如果 auto-resume 本身的 _submit 失败，run 保留在 needs_human，用户仍可手动继续。
  - auto_resume 使用 run.auto_resumed 事件类型（而非 human.continued），前端轮询会正常获取状态变化。
  - 崩溃窗口：store.update(queued) 成功 → _mark_followups_applied 之间崩溃时，content 已在 history 但未标 applied，下个安全节点可能重复并入。方向是「重复」不是「丢失」。
  - _expire_unconsumed_followups 只在 _collect_pending_followups 返回非空时写入，对无 pending 的 run 无副作用。

---
## 监工修正（e6660be）

缺陷：needs_human 的设置点除 _fail 外还有 recovery.py:151（服务重启把执行中任务置 needs_human），该路径没有触发自动消费，重启后带未消费 pending 的任务会永久停等用户手动继续。

修正：
  - recovery.py recover() 末尾（_ensure_scheduler 之后），扫描所有 needs_human 且有 pending followup 的 run，逐个调 _auto_resume_with_followups。
  - 使用延迟 import 避免 run_lifecycle ↔ recovery 循环导入。
  - recovery.py:89 的 requirement_analysis 中断路径语义不同（走 budget_resume），设为 needs_human 但无 plan/artifacts，_auto_resume_with_followups 正确跳过——不挂自动消费。
  - 删除 _auto_resume_with_followups 中不可达的 `if run['status'] == 'cancelled'` 死分支。

回归测试：
  - test_service_restart_auto_resumes_needs_human_with_pending：run 执行中有 pending → store.recover() 置 needs_human → svc.recover() 无用户 API 调用下 auto-resume，事件含 run.auto_resumed + pending_ids
  - test_service_restart_does_not_resume_requirement_analysis_interrupted：requirement_analysis 中断后 needs_human + pending → auto-resume 正确跳过

检查命令与退出码：
  - uv run pytest tests/test_auto_consume.py tests/test_run_followup.py -q → 31 passed, 退出码 0
  - uv run pytest tests/test_control_app.py tests/test_autonomous_service.py tests/test_continuous_service.py -q → 67 passed, 退出码 0

---
## 监工回执

对应任务与代码状态：T08 / webuddy-r2-t08 分支 1e1e925+e6660be，基线 5018e7d。
范围、设计一致性、真实数据、证据检查结论：契约④全项落实。自动消费触发点两处：_run 失败置 needs_human 后 + 服务恢复收尾统一扫描（后者覆盖所有重启形态，优于逐点挂接）；取消绝不自动恢复；正常完成→expired 独立态且不吞已 applied；逐条 pending_id 徽标弃全局列表；崩溃方向沿用重复非丢失。fake runner 证据如实标注，真实模型联测清单已附。定向 31+67 通过。死分支已清。
状态：local_reviewed
必须修复的问题：无。
下一任务或移交 Codex 的内容：集成后真实模型联测（回执 10 步清单）；线上长任务验收归 Codex。
