任务 ID / 状态：T06 / implementation_reported
实际执行模型：Opus 4.6 (1M context), claude-opus-4-6[1m]
实际基线 → 当前提交或 diff：6c27225 → b98cdc8

用户行为发生了什么变化：
- /settings/runtime 页面内「服务器连接」区改为「公司测试环境」，带 id="company-environment" 锚点
- 无目标时：显示「尚未配置公司测试环境」空态，附引导文字和「注册第一个目标」按钮
- 有目标时：每个目标显示名称、user@host:port、部署公钥指纹、最近连通检查结果（通过/未验证+原因）及检查时间
- 未检查过的目标显示「未检查」而非假在线
- 手动点击「检查连接」触发真实 test 端点，检查中显示「检查中…」，完成后就地更新结果
- 加载态显示「正在读取服务器配置…」

改动文件与范围偏差：
- factory/control/deploy_targets.py — 新增 last_checks() 方法（只读，从 deploy_target_audit 表查询最近 target.test 记录）
- factory/control/remote_routes.py — 新增 GET /api/v2/deploy-targets/checks 只读端点（admin 权限）
- frontend/src/workbench/ServerTargets.tsx — 空态/加载态/检查结果显示/锚点
- frontend/src/workbench/ServerTargets.test.tsx — 12 项测试全覆盖
- 范围偏差：无。新增了一个 GET 只读端点（非写接口），属任务单允许范围「后端仅当缺只读查询时在 remote_routes.py 补最小 GET」。

检查（命令 / 环境 / 退出码 / 日志路径）：
1. `cd frontend && npx vitest run src/workbench/ServerTargets.test.tsx` → 退出码 0，12 passed
2. `cd frontend && npx tsc --noEmit` → 退出码 0，无输出
3. `uv run pytest tests/test_remote_targets.py -q` → 退出码 0，26 passed 1 skipped

实际点击与截图（真实服务或测试夹具）：
- 组件测试使用 vitest + jsdom + @testing-library/react，API 层 mock（fake，如实标注）
- 真实进程验证（完成条件第 4 条）：未在本次执行中启动临时后端服务做端到端截图。原因：工作区 .venv 可用但 FACTORY_DEPLOY_KEY_DIR 未配置，无法在单次会话中安全启动完整服务端。此项留给 Codex 验收或规划者指定环境。

仍未验证的能力：
- 真实后端服务端到端：临时库起服务 + 注册目标 + 页面真实调 API 截图（完成条件第 4 条部分）
- 桌面与手机断点不遮挡输入的视觉验证（需真实浏览器）

下一任务需要知道的事实：
- GET /api/v2/deploy-targets/checks 是新增的只读端点，返回 { checks: { [target_id]: { status, reason, checked_at } } }
- ServerTargets 组件标题改为「公司测试环境」，按钮文案从「测试连接」改为「检查连接」
- 锚点 #company-environment 已就位，SettingsPage.tsx 的「运行配置」链接可附加此锚点
