任务 ID / 状态：T11 / implementation_reported
实际执行模型（能核对则填；否则注明无法核对）：无法核对（由父代理调度）
实际基线 -> 当前提交或 diff：66e4f71 -> 见下方提交
用户行为发生了什么变化：含空格的合法单元素可执行路径（绝对或相对）不再被 shlex.split 拆坏；命令字符串拆分、结构化报错、NUL 检查等 T07 行为全部保留。
改动文件与范围偏差：
- factory/control/execution.py：`_normalize_check_argv` 和 `_check_argv` 新增 `root` 关键字参数；三处调用点传入 `root`。无范围偏差。
- tests/test_control_execution.py：新增 `TestCheckArgvSpacedPaths` 类（4 条测试）。

存在性判定放在哪一层及理由：
放在 `_normalize_check_argv`（归一化层），而非 `_check_launch_error`（启动前检查层）。原因：归一化在启动检查之前运行，一旦 argv 被错误拆分，后续所有层拿到的都是坏数据，无法逆转。绝对路径直接 `Path.is_file()`；相对路径含 `/` 时通过 `root / candidate` 解析后检查。不含 `/` 的纯命令名（如 `ruff`）不触发此路径——它没有目录分隔符，不可能是文件系统路径。`root` 由 `_check_argv` 的调用方传入，三处调用点（任务前检查、验证后检查、集成检查）均已持有 `child_root` 或 `integration_path`。

新测试名：
- `TestCheckArgvSpacedPaths::test_absolute_path_with_space_preserved`
- `TestCheckArgvSpacedPaths::test_absolute_nonexistent_path_with_space_is_split`
- `TestCheckArgvSpacedPaths::test_relative_path_with_space_resolved_against_root`
- `TestCheckArgvSpacedPaths::test_real_subprocess_with_spaced_path`

修复前失败证据：
```
>>> _normalize_check_argv('test', ['/tmp/My Tools/checker'])
['/tmp/My', 'Tools/checker']   # 被拆成两元素
```
```
$ uv run pytest tests/test_control_execution.py::TestCheckArgvSpacedPaths -q
FFFF
TypeError: _check_argv() got an unexpected keyword argument 'root'
（4 条全部失败——修复前 _check_argv 不接受 root 参数）
```

检查（命令 / 环境 / 退出码 / 日志路径）：
```
$ uv run pytest tests/test_control_execution.py -q
40 passed in 22.50s    # 退出码 0
```
其中 T07 原有 13 条（TestCheckArgvNormalization 11 + TestCheckArgvRealSubprocess 2）全部通过。

实际点击与截图：无（纯后端子进程逻辑）
仍未验证的能力：无
下一任务需要知道的事实：`_check_argv` 和 `_normalize_check_argv` 的签名现在多了 `root: Path | None = None` 关键字参数，向后兼容（不传则走原来的 shlex.split 路径）。
