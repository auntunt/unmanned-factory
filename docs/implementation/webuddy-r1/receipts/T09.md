任务 ID / 状态：T09 / implementation_reported
实际执行模型：无法核对（非交互会话）
实际基线 → 当前提交或 diff：f209a85 → 9bbd975
用户行为发生了什么变化：
- 通过正常创建流程（需求分析→规格确认）进入的任务，delivery_type 从需求文本和 spec_draft 自动推导并持久化为 run.delivery_type_inferred
- 推导规则：关键词匹配，service/cli/installer 三类各有独立词表；恰好匹配一类时写入，多类或零匹配返回 null
- 推导结果在 derive_delivery_type() 中作为第二优先级（显式 artifacts.delivery_type > run.delivery_type_inferred > 物件推断 > null）
- Deliverables.tsx（workbench 详情页）新增交付类型展示区，规则与 RunWorkspace 的 DeliverySection 一致
- 两处展示共用 delivery-constants.ts 中的 DELIVERY_TYPE_LABEL 常量
- 旧任务（无 delivery_type_inferred 且 artifacts 无显式声明）显示「未声明」

改动文件与范围偏差：
- factory/control/deliverables.py：新增 infer_delivery_type_from_text()、DELIVERY_TYPES 常量；derive_delivery_type() 增加 run-level 推断层
- factory/control/requirement_analysis.py：confirm() 中调用推导并存入 delivery_type_inferred（3 行改动）
- frontend/src/conversation/delivery-constants.ts：新建共享常量文件
- frontend/src/conversation/RunWorkspace.tsx：DeliverySection 改用共享常量导入，删除本地重复定义
- frontend/src/workbench/Deliverables.tsx：Catalog 类型扩展、新增 DeliverableDeliverySection 组件
- frontend/src/workbench/Deliverables.test.tsx：新建 6 条测试
- tests/test_control_deliverables.py：新增 7 条测试（含参数化）
- tests/test_requirement_analysis.py：新增 3 条测试
- 范围偏差：无。未改 run_execution.py / run_lifecycle.py。

检查（命令 / 环境 / 退出码 / 日志路径）：
1. python -m pytest tests/test_control_deliverables.py -v → 退出码 0，23 passed
2. python -m pytest tests/test_requirement_analysis.py::test_confirm_stores_delivery_type_inferred_from_request_text tests/test_requirement_analysis.py::test_confirm_stores_cli_delivery_type_from_request tests/test_requirement_analysis.py::test_confirm_stores_service_from_spec_draft_goal -v → 退出码 0，3 passed
3. cd frontend && node_modules/.bin/tsc --noEmit → 退出码 0
4. cd frontend && npx vitest run src/conversation/RunWorkspace.test.tsx src/workbench/Deliverables.test.tsx → 退出码 0，24 passed
5. cd frontend && node_modules/.bin/tsc -b && npx vite build → 退出码 0

实际点击与截图：无（测试夹具验证，非真实服务）
仍未验证的能力：
- 真实浏览器下端到端流程（创建任务→规格确认→执行→结果卡展示一致）
- installer_targets 实际来源（需产品流程补齐目标系统选择，非本单范围）
- 非 general 操作类型（bugfix/release 等）不经过 requirement_analysis.confirm()，delivery_type_inferred 不会被写入，这些路径如需推导需另行处理

下一任务需要知道的事实：
- delivery_type_inferred 是 run 级字段，随 store.update 持久化，不在 artifacts 中（artifacts 在 clarify 时重置，delivery_type_inferred 保留直到下次 confirm）
- 非 general 操作的 delivery_type 仍依赖执行器显式写入 artifacts.delivery_type 或物件推断
- Deliverables.tsx 的交付类型展示复用 listing API 已有的 delivery_type/installer_targets 字段，无新 API

---
## 监工修正：non_goals 误作正向匹配

问题：infer_delivery_type_from_text 把 spec_draft.non_goals 并入正向文本，导致「做个网站，不要做成桌面应用」同时命中 service 与 installer 返回 None。
修正：non_goals 中命中的关键词作为排除信号（negative wins over positive），从 matches 集合中减去而非加入。in_negative 优先于 in_positive 判断，避免同一词出现在请求文本与 non_goals 时产生假阳性。
新增测试：
- test_infer_non_goal_excludes_rather_than_matches：「做个网站，不要做成桌面应用」→ service
- test_infer_non_goal_cli_excluded_service_survives：non_goals「不做 CLI」+ goal「做网站」→ service
- test_infer_non_goal_alone_does_not_produce_type：non_goals 单独命中不产生类型
改动文件：factory/control/deliverables.py、tests/test_control_deliverables.py
提交：9573cca
检查：python -m pytest tests/test_control_deliverables.py tests/test_requirement_analysis.py -q → 退出码 0，93 passed

---
## 监工回执

对应任务与代码状态：T09 / webuddy-r2-t09 分支 9bbd975+9573cca，基线 5018e7d。
范围、设计一致性、真实数据、证据检查结论：契约⑤落实——写入点在 requirement_analysis.confirm（真实用户流程），推导保守（唯一正向命中才定型，non_goals 为负向减除，含混即 null 不伪造）；delivery-constants.ts 保证 RunWorkspace 与 Deliverables 标签一致；旧任务 null→未声明。测试走真实推导/写入路径。定向后端 93、前端 24 通过，tsc/build 干净。
状态：local_reviewed
必须修复的问题：无。
已登记缺口（非本轮扩展）：bugfix/release 等非 general 操作不经 confirm，无 inferred 写入，仍靠显式 artifacts 或物件推断——列入 handoff 后续小项。
下一任务或移交 Codex 的内容：真实模型流程中 spec_draft 实际形状下的推导表现，随集成联测观察。
