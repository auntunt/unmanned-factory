任务 ID / 状态：T12 / implementation_reported
实际执行模型：无法核对（由宿主环境决定）
实际基线 -> 当前提交或 diff：dff4931 -> 见下方提交哈希

用户行为发生了什么变化：
- 英文 ASCII 关键词现在按词边界匹配，'Build a client portal' 不再因 client 含 cli 子串被误判为 cli。
- 负向信号（non_goals）只对交付形态关键词生效；部署/上线类措辞（线上服务、在线服务、api 服务、后端服务、服务端）出现在 non_goals 中不再抹掉对应的 service 类型。
- 保守策略不变：唯一正向命中才定型，真歧义仍 None。

改动文件与范围偏差：
- factory/control/deliverables.py：关键词拆为 _*_FORM（交付形态）与 _*_DEPLOY（部署拓扑）两层；新增 _kw_in_text() 按 ASCII/CJK 分别用 \b 词边界或子串匹配；infer_delivery_type_from_text 的正向侧用全关键词 + 词边界，负向侧只用 form 关键词。
- tests/test_control_deliverables.py：新增 3 条测试。
- 无范围偏差。

检查（命令 / 环境 / 退出码 / 日志路径）：
```
uv run pytest tests/test_control_deliverables.py tests/test_requirement_analysis.py -q
96 passed, 2 warnings in 35.73s
退出码 0
```

修复前失败证据（红测试输出）：
```
FAILED test_english_word_boundary_client_not_cli
  assert 'cli' is None
   +  where 'cli' = infer_delivery_type_from_text('Build a client portal')

FAILED test_deploy_non_goal_does_not_negate_service
  assert None == 'service'
   +  where None = infer_delivery_type_from_text('做一个网站', {'goal': '做一个网站', 'non_goals': ['部署到线上服务'], ...})

2 failed, 1 passed (退出码 1)
```

实际点击与截图：无前端改动，纯后端逻辑修复。
仍未验证的能力：无。
下一任务需要知道的事实：关键词分组结构已变（_SERVICE_FORM/_SERVICE_DEPLOY 等），后续如需扩充关键词须放入正确的层。

---
## 监工回执

对应任务与代码状态：T12 / webuddy-r2-t09 分支 2aad10a，基线 eff65ec 已合并的 T09 之上。
结论：R2-02 两个复现场景均先红后绿；关键词分层（form=形态、deploy=部署拓扑）把「不部署」与「交付形态」解耦，负向仅作用于 form；ASCII 词边界匹配消除 client→cli 类误判；歧义 None 保守策略与既有测试全部保留。定向 96 通过。
状态：local_reviewed
