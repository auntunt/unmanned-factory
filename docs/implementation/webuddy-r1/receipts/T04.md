任务 ID / 状态：T04 / implementation_reported
实际执行模型：Opus 4.6 (1M context)，无法从环境独立核对
实际基线 → 当前提交或 diff：d28aec8 → 2d8355e（3 次代码提交）+ 本回执提交

用户行为发生了什么变化：

1. **能力声明通用呈现**：CapabilityPanel 从 tool_contract 读取输入上限（max_input_bytes）、超时（timeout_seconds）、用途描述（purpose）和支持范围（support_matrix），不再假设特定业务。无声明时显示"能力声明未加载"而非猜测。
2. **四类失败态分别渲染**：error（执行失败）、cancelled（已取消）、timeout（处理超时）、unknown（状态未知）各有独立标签、文案和 CSS 类名，不合并为统一红字。
3. **支持范围折叠展开**：按 supported/partial/unsupported 三种状态分色显示，默认折叠。
4. **客户端文件大小预检**：上传前检查 max_input_bytes 声明，超限直接拒绝并提示，不发请求。
5. **历史恢复**：挂载时查询最近调用结果并回填，刷新后不丢失上次处理结果。
6. **通用文案**：按钮从"上传文件并转换"改为"上传文件"，不假设 MFD 或 CSV。
7. **MFD 包定位不变**：其 manifest 中 purpose 写明"清单局部提取"，UI 原样呈现，无业务值伪装。

改动文件与范围偏差：

| 文件 | 变化 |
|---|---|
| factory/control/capability_packs.py | bindings() 增加 tool_contract 字段（从 pack_versions 读 manifest） |
| frontend/src/workbench/pack-types.ts | 新增 ToolContract 接口、TASK_STATUS_LABEL、formatBytes |
| frontend/src/conversation/CapabilityPanel.tsx | 按声明通用呈现、四类失败态、历史恢复、客户端预检 |
| frontend/src/conversation/conversation.css | 新增 support-matrix、constraints、四类失败态样式 |
| frontend/src/conversation/CapabilityPanel.test.tsx | 13 项测试覆盖全部新行为 |

范围偏差：无。后端改动限于 bindings 查询的只读字段暴露（新增 tool_contract），未改 pack_runtime/pack_sandbox 隔离层。

检查（命令 / 环境 / 退出码 / 日志路径）：

| 命令 | 退出码 |
|---|---|
| `cd frontend && npx vitest run src/conversation/CapabilityPanel.test.tsx` | 0（13 passed） |
| `cd frontend && npx vitest run` | 0（331 passed / 46 files） |
| `uv run pytest tests/test_pack_api_durability.py tests/test_capability_packs.py -q` | 0（38 passed） |

## 真实进程全链路实测（完成条件 1）

使用临时 SQLite 库 + Starlette TestClient（走完整 ASGI 中间件/CSRF/Cookie 链路），
csv-quote-xml 包从候选到下载结果，真实子进程隔离执行，不 mock。

脚本路径（临时，非仓库文件）：scratchpad/t04_e2e.py

### 每步 HTTP 状态码

| 步骤 | HTTP | 关键字段 |
|---|---|---|
| login | 200 | csrf_token 获取成功 |
| dev_run（前置：模拟开发产物） | — | run 创建 + snapshot 保存 |
| create_draft | 201 | lifecycle=draft, manifest_present=True |
| start_evaluation | 202 | job 状态=running |
| eval_done（轮询完成） | 200 | status=completed, passed=True |
| publish | 201 | version=1 |
| create_agent | 201 | T04测试角色 |
| bind | 201 | pack_name=CSV 工程量清单 → XML, version=1 |
| verify_bindings（含 tool_contract） | 200 | max_input_bytes=4194304, timeout=30, support_matrix_count=4 |
| invoke（上传 sample_quote.csv） | 202 | task status=queued, tool=csv_quote_xml |
| execution（轮询完成） | 200 | status=succeeded, validation=passed |
| events | 200 | task.queued → task.running → task.succeeded |
| download | 200 | quote.xml, x-validation-status=passed |
| environment-check | 200 | sandbox=seatbelt, isolation_verified=True |

### 任务事件流

```
task.queued → task.running → task.succeeded
```

### 结果文件

- 文件名：quote.xml
- 大小：428 bytes
- sha256：`e70662b1ca7114d2281cfa1df676280bf0a585f4f26b13a9e9eb82340e84eceb`
- x-validation-status 响应头：passed
- 逐 Item 行比对 fixtures/expected_quote.xml：一致
- 内容摘要：`<Quote project="未命名工程" total="18920.70" itemCount="3">` 包含 3 条清单记录（A-001 现浇混凝土 C30 / A-002 钢筋 HRB400 / A-003 模板）

### 隔离后端

- evidence.sandbox = **seatbelt**
- environment-check: status=ready, sandbox=seatbelt, isolation_verified=True

### tool_contract 字段验证（T04 新增）

bindings API 返回的 tool_contract 字段经实测确认：
- permissions.max_input_bytes = 4194304（4 MB）
- timeout_seconds = 30
- support_matrix 含 4 条记录（与 webuddy-pack.json 一致）
- purpose 前 40 字符 = "把 UTF-8 的工程量清单 CSV（编号/名称/单位/数量/单价）转换为结构化"

### 结果摘要

- item_count = 3
- total = 18920.70
- 真实进程执行，seatbelt 隔离，全链路通过

## 仍未验证的能力

- 超时态的端到端行为——测试中通过 mock 验证了 UI 逻辑，但 180s poll 超时未做真实网络测试
- 暗色模式下新样式的视觉验证

## 下一任务需要知道的事实

- bindings API 新增了 `tool_contract` 字段（可空），包含 permissions、support_matrix、purpose、timeout_seconds、input_schema、output_schema
- CapabilityPanel 现在额外请求 GET /invocations 做历史恢复
- TaskStatus 类型新增了前端标签映射 TASK_STATUS_LABEL
- csv-quote-xml 全链路真实进程实测通过，隔离后端为 seatbelt
