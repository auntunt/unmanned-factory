任务 ID / 状态：T14 / implementation_reported
实际执行模型（能核对则填；否则注明无法核对）：claude-opus-4-6（无法核对）
实际基线 -> 当前提交或 diff：e19b50f -> 见本次提交
用户行为发生了什么变化：平台指示写入的 .webuddy/coding-progress.md 不再因未声明而导致 scope reconciliation 判 fail；其余 .webuddy 下文件和业务源码仍受范围管控。符号链接冒充该路径时不豁免。
改动文件与范围偏差：
- factory/control/scope_declaration.py -- exempt() 增加 _PLATFORM_PROGRESS_NOTE 精确路径豁免，带符号链接检查；evidence() 传入 workspace 参数
- tests/test_scope_declaration.py -- 新增 3 个测试

方案选择：方案 A（窄豁免），理由：
1. 该文件由平台 _INSTRUCTIONS 指示 worker 写入，属于平台管理元数据，exempt() 已有 .spec/ 和测试文件同类豁免
2. 方案 B 需要在声明侧注入伪事件（scope_declaration event），污染审计流——声明只应来自 worker 助手消息
3. 精确路径匹配（== 而非 startswith），不扩大到 .webuddy/ 目录
4. 符号链接检查通过 workspace 参数解析完整路径判断 is_symlink()

检查（命令 / 环境 / 退出码 / 日志路径）：

修复前失败（红）：
```
$ uv run pytest tests/test_scope_declaration.py::test_coding_progress_exempt_from_scope -q
FAILED tests/test_scope_declaration.py::test_coding_progress_exempt_from_scope
  AssertionError: expected pass, got fail: undeclared=['.webuddy/coding-progress.md']
1 failed, 2 warnings
```

修复后（绿）：
```
$ uv run pytest tests/test_scope_declaration.py -q
16 passed, 2 warnings in 5.12s
```
退出码 0。

新测试名：
- test_coding_progress_exempt_from_scope -- .webuddy/coding-progress.md 豁免，scope pass
- test_other_webuddy_files_still_undeclared -- .webuddy/evil.py 不豁免，scope fail
- test_coding_progress_symlink_not_exempt -- 符号链接冒充进度笔记，不豁免，scope fail

仍未验证的能力：服务器真实 continuous execution 场景
下一任务需要知道的事实：exempt() 新增可选 workspace 参数，无 workspace 时跳过符号链接检查（向后兼容）
