任务 ID / 状态：T01 / implementation_reported
实际执行模型（能核对则填；否则注明无法核对）：claude-opus-4-6[1m]（宿主会话可核对）
实际基线 → 当前提交或 diff：f81622a → 无产品代码变更；HEAD 实际为 6e8e9e9（规划者在 f81622a 之上追加了两个 docs-only 提交）
用户行为发生了什么变化：无，本任务为只读探针
改动文件与范围偏差：仅本回执文件，符合任务单

## 逐步现状判定（V1-01 → V1-04，按用户遇到顺序）

### V1-01 单一入口与连续任务 — 部分接通，一处断点

**已接通段落：**

| 段落 | 证据 |
|---|---|
| 服务端入口路由（一句话 → 开发/角色聊天/追问/不可用） | `factory/control/routing.py` classify()；单元测试 `tests/test_entry_routing.py` 21 passed，`tests/test_routing.py` 含同文件 |
| 项目创建 + run 提交 | `POST /api/v2/projects` + `POST /api/v2/runs`；真实进程验证：临时 SQLite 库 + uvicorn 18799 端口，curl 创建项目 → 201，创建 run → 201（status=received），数据持久化至 control.db |
| 状态持久化与刷新恢复 | SQLite WAL 模式；`GET /api/v2/runs/{id}` 在 run 失败后仍可读取完整状态（status/error/progress/source）；前端 `RunWorkspace.tsx` 有 pollMs 轮询 + `run-state.ts` 状态机 |
| 前端 StartChat → 路由决策 → RunWorkspace | `StartChat.tsx` 调 `/route` 获取 decision → 按 kind=development 创建 project+run → navigate 到 RunWorkspace |
| 操作幂等（opRef / idempotency_key） | StartChat 用 `entry-recovery.ts` 维护 per-tab opRef，run 提交带 idempotency_key |

**断点 B1（阻塞后续所有步骤）：**

> 执行器凭据缺失 — run 进入 received → 自动触发 requirement_analysis → 调用 Claude Code SDK → 返回 exit code 1: "Not logged in · Please run /login"

- 复现：启动 `factory-web serve`（临时 FACTORY_CONTROL_DATA），curl 创建 project + run → run 自动尝试执行 → 5 秒内状态变为 `needs_human`，error 字段含上述消息
- 环境要求：需 `claude` CLI 已登录（`claude /login`）或 Codex token（`OPENAI_CODEX_TOKEN`）
- 不 mock 冒充跑通：任务单明确要求真实进程验证；以上是真实进程的确切输出
- 对 T02 的建议：此非代码缺陷，是运行环境前置条件。需要：(1) 在探针环境中完成 `claude /login` 或配置 provider token，(2) 重新提交 run 验证 planning → execution → verifying 全链

### V1-02 成熟 Coding 执行器 — 未能验证，被 B1 阻塞

- 代码走读确认：执行链 `service.py` → `run_execution.py` → `providers.py` SDKRunner → 实际调用 Claude Code / Codex / DeepSeek SDK
- 配置路径：`factory/control/runtime.py` RuntimeSettings，`configured_profiles()` 读 `FACTORY_*_PROVIDER` / `FACTORY_*_MODEL` 环境变量
- 测试覆盖：`tests/test_runtime_execution.py`、`tests/test_continuous_service.py`（36 passed）均使用 stub runner，不调用真实模型
- 判定：代码路径存在且被 stub 测试覆盖；真实模型执行未验证（被 B1 阻塞）

### V1-03 真实交付（GitHub + 部署目标 + 固定公司测试地址）— 代码存在，真实端未验证

**GitHub 发布：**

| 组件 | 文件 | 测试 | 结果 |
|---|---|---|---|
| 仓库绑定 + 创建 + 推送 | `factory/control/github_publication.py` | `tests/test_github_publication.py` 16 passed | stub Publisher；无真实 GitHub API 调用 |
| Git 操作（branch/commit/push） | `factory/control/github.py` | `tests/test_control_github.py` 16 passed | 本地 git 操作真实；push 被 stub |
| 仓库列表/选择 | agent_routes /route + publication options | `tests/test_github_repositories.py` 5+ cases | stub Publisher |

**部署目标：**

| 组件 | 文件 | 测试 | 结果 |
|---|---|---|---|
| 目标注册 + 密钥管理 + 权限审计 | `factory/control/deploy_targets.py` | `tests/test_remote_targets.py` 26 passed, 1 skipped | 密钥生成真实；SSH 调用使用 fake-ssh 脚本 |
| 远程执行（health_check/deploy/rollback） | `factory/control/remote_targets.py` | 同上 | fake SSH + 超时测试真实子进程；无真实服务器连接 |
| 远程路由 HTTP 端点 | `factory/control/remote_routes.py` | 同上 | 通过 TestClient |

**断点 B2：**

> GitHub 发布和远程部署的测试全部基于 stub/fake，没有真实 GitHub token 或真实部署服务器的端到端证据。

- 对 T02 的建议：(1) 配置 `FACTORY_GITHUB_TOKEN` 指向真实 token，用一个私有测试仓库验证 create → push → URL 返回；(2) 配置一台真实测试服务器（或本机 SSH localhost），注册为 deploy_target，验证 health_check → deploy → 地址可访问

### V1-04 同任务修改 — 接口接通，执行被 B1 阻塞

**已接通段落：**

| 段落 | 证据 |
|---|---|
| follow-up API | `POST /api/v2/runs/{id}/follow-up` → 真实进程验证：返回 `{"recorded":true,"applied":true,"queued":false,"run_id":"...","status":"received"}`；run 重新进入 received 并再次尝试执行 |
| follow-up 单元测试 | `tests/test_run_followup.py` 23 passed |
| 前端 RunWorkspace 修改入口 | `RunWorkspace.tsx` composer 区域根据 `composerMode(run)` 判断显示 follow-up 输入框 |
| 历史保留 | run.history 数组保存前轮 request |

**断点 B3（= B1 的延续）：**

> follow-up 成功排队并重新触发执行，但执行器仍缺凭据，再次 needs_human。无法验证"保留前一轮成果 + 完成新一轮"的完整路径。

## 断点汇总（按用户遇到顺序）

| # | 断点 | 阶段 | 阻塞点 | 最小修复范围（给 T02） |
|---|---|---|---|---|
| B1 | 执行器凭据缺失 | V1-01 创建任务后自动执行 | Claude Code 未登录 / 无 provider token | 在探针或 CI 环境完成 `claude /login` 或设 `FACTORY_CODEX_PROVIDER` + token；重跑 run 验证 planning→execution→verifying |
| B2 | GitHub + 部署只有 stub 覆盖 | V1-03 | 无真实 GitHub token、无真实部署服务器 | 配置真实 `FACTORY_GITHUB_TOKEN` + 一个私有测试 repo；配一台可 SSH 到达的服务器注册为 deploy_target |
| B3 | 同任务修改执行不可验证 | V1-04 | B1 的延续 | 解决 B1 后自然可验证 |

## 检查（命令 / 环境 / 退出码 / 日志路径）

| 命令 | 工作目录 | 退出码 | 备注 |
|---|---|---|---|
| `uv run pytest tests/test_entry_routing.py tests/test_routing.py -q` | v3-skills-icons | 0 | 21 passed |
| `uv run pytest tests/test_github_publication.py -q` | v3-skills-icons | 0 | 16 passed |
| `uv run pytest tests/test_remote_targets.py -q` | v3-skills-icons | 0 | 26 passed, 1 skipped |
| `uv run pytest tests/test_control_github.py -q` | v3-skills-icons | 0 | 16 passed |
| `uv run pytest tests/test_github_repositories.py tests/test_app_route_contract.py tests/test_run_followup.py -q` | v3-skills-icons | 0 | 23 passed |
| `uv run pytest tests/test_continuous_service.py -q` | v3-skills-icons | 0 | 36 passed |
| 真实进程：`factory-web serve --port 18799`（临时 SQLite） | v3-skills-icons | 正常运行 | FACTORY_CONTROL_DATA 指向 /tmp 临时目录 |
| 真实进程：`curl POST /api/v2/projects` | — | HTTP 201 | 项目创建成功 |
| 真实进程：`curl POST /api/v2/runs` | — | HTTP 201 | run 创建成功，自动触发执行 |
| 真实进程：`curl GET /api/v2/runs/{id}` | — | HTTP 200 | status=needs_human，error="Not logged in" |
| 真实进程：`curl POST /api/v2/runs/{id}/follow-up` | — | HTTP 200 | recorded=true, applied=true |

服务器日志路径：`/private/tmp/claude-501/.../scratchpad/probe-wvhn/server.log`（会话临时目录，非持久）

## 实际点击与截图

无前端截图（任务单允许范围为 API 级探针；前端构建和浏览器验证不在 T01 范围内）。

## 仍未验证的能力

1. 真实模型执行（planning → execution → verifying → 产出 artifacts）— 被 B1 阻塞
2. 真实 GitHub 发布（create repo / push / 返回 URL）— 被 B2 阻塞
3. 真实远程部署（SSH → deploy → health_check → 固定地址可访问）— 被 B2 阻塞
4. 同任务修改的完整闭环（follow-up → 新一轮执行 → 新提交 → 同地址更新）— 被 B1+B2 阻塞
5. 前端 StartChat → RunWorkspace 真实浏览器交互 — 不在 T01 范围

## 下一任务需要知道的事实

1. 服务可正常启动并通过 API 完成 project + run 创建 + follow-up 全流程（SQLite 持久化正常），断在执行器凭据
2. 执行器配置路径：`FACTORY_*_PROVIDER` + `FACTORY_*_MODEL` 环境变量 → `RuntimeSettings` → `SDKRunner`；默认 provider 是 codex
3. 所有 GitHub/deploy 测试使用 stub，通过率 100% 但不证明真实连接
4. `routing.classify()` 是纯函数，完全被单元测试覆盖（21 cases）
5. run 的自动执行是同步触发的（在 `POST /api/v2/runs` 的响应路径中 `svc.start_plan(rid)` 启动后台线程）
6. follow-up 机制工作正常：applied=true 后 run 重新进入 received 并再次触发执行

---
## 监工回执

对应任务与代码状态：T01 / 探针，无产品代码变更；批次记录基线 6e8e9e9。
范围、设计一致性、真实数据、证据检查结论：范围合规（只写回执）；真实进程证据与 stub 覆盖如实分层，未用 mock 冒充真实端；断点 B1/B2/B3 定位可信。注意：B1「Not logged in」是探针子进程环境的凭据可见性问题，本机宿主 claude CLI 正常登录，T02 先复核 provider 配置在服务进程中的实际生效路径，不把环境问题当代码缺陷修。
状态：local_reviewed
必须修复的问题：无（探针任务）。
下一任务或移交 Codex 的内容：T02 用本机已有执行器凭据真实穿链（V1-01/02/04 本地段）；B2 的真实 GitHub 发布沿用已有授权尝试，真实服务器部署段留给 Codex。
执行模型备注：宿主子任务自报 claude-opus-4-6[1m]，与规划者请求的 sonnet 不一致；据实记录。后续任务单改由用户的独立 Sonnet 会话执行。
