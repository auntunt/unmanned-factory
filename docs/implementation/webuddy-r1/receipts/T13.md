任务 ID / 状态：T13 / implementation_reported
实际执行模型（能核对则填；否则注明无法核对）：无法核对（宿主环境未暴露）
实际基线 → 当前提交或 diff：7a878b6 → 见下方提交号

用户行为发生了什么变化：
验收模型返回「说明段 + 唯一 JSON 代码围栏」时不再被判 invalid_response，能正常进入 coverage/fidelity/scope 等既有规则。纯 JSON 与全文围栏两种旧形式保持兼容。多个 JSON 代码块、非法 verdict 值、非字符串 reason 仍被拒绝。

改动文件与范围偏差：
- factory/control/verification.py：新增 `_extract_verdict_json` 和 `_validate_verdict_fields` 两个纯函数（+import re）；调用点从 8 行内联逻辑替换为 `_extract_verdict_json(result.text)`。无范围偏差。
- tests/test_verification_response_parsing.py（新增）：12 条测试覆盖三种合法形式、四种拒绝形式、以及解析后 coverage 字段不被短路。

复用说明：
仓库已有 `planning._parse_json`（fullmatch 围栏）和 `scope_declaration.payload`（raw_decode 回退），但两者契约不同（planning 不处理说明段；scope_declaration 提取 scope_declaration 键且接受行首 `{`）。新函数使用独立 `_FENCE_RE` 正则，只接受显式代码围栏内的完整 JSON 对象，不做行首 `{` 回退搜索，以避免跨对象拼接风险。

检查（命令 / 环境 / 退出码 / 日志路径）：

修复前失败证据（ImportError 即函数不存在 → 旧逻辑对说明段+围栏形式报 invalid_response）：
```
$ uv run pytest tests/test_verification_response_parsing.py -q
E   ImportError: cannot import name '_extract_verdict_json' from 'factory.control.verification'
1 error in 0.34s
```

修复后：
```
$ uv run pytest tests/test_verification_response_parsing.py -q
12 passed in 0.12s
```

既有测试回归：
```
$ uv run pytest tests/test_active_verification.py tests/test_verifier_request_contract.py tests/test_verification_evidence.py -q -m "not smoke"
22 passed, 3 skipped in 6.61s
```

实际点击与截图：不适用（纯后端解析函数）。
仍未验证的能力：服务器端真实模型回复端到端链路（需 Codex 在线复验）。
下一任务需要知道的事实：`_extract_verdict_json` 已导出为模块级名称，可被其他测试直接 import。

---
## 监工回执
对应任务与代码状态：T13 / webuddy-r2-t13 分支 7c486f5。
结论：提取层独立纯函数（唯一围栏语义、多块/冲突/非法字段拒绝），接线单点替换，coverage/fidelity/scope 下游未动，invalid_response 路径保留。规划者补做行为级变异验证：旧逻辑对真实形式 JSONDecodeError → invalid_response，新解析 verdict=pass 结构完整。12+22 定向通过。未复用既有解析函数的理由成立（契约不符）。
状态：local_reviewed
