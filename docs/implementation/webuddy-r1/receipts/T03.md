任务 ID / 状态：T03 / implementation_reported
实际执行模型（能核对则填；否则注明无法核对）：无法核对（SDK 不报告模型 ID）
实际基线 → 当前提交或 diff：1424e2f → e097ccf（两次提交：a580537 实现 + e097ccf 测试）
用户行为发生了什么变化：
  - ACTIVE 期补充不再要求重新提交。响应从 queued:false + "请再次提交" 改为 queued:true + "将在下一个安全节点自动并入任务"。
  - 补充以 followup.pending 事件持久化，包含 id/content/actor_id/actor/fingerprint/created_at。
  - clarify() 与 continue_run() 在安全节点消费未应用的 pending，合并内容后传入下一轮，记录 followup.applied {pending_id, run_revision}。已消费项不重复消费。
  - GET /api/v2/runs/{rid} 新增 followups 列表（id/content/created_at/applied），供前端渲染。
  - 前端 ThreadMessage 对 followup=true 的消息显示「待应用」或「已应用」徽标，状态来自 run response 的 followups 列表。
  - composer hint 从"重新提交"语义改为"自动并入"语义。
  - 取消不清除未消费 pending（事件不可变）。
  - 幂等：同 key 同内容重放原回执；同 key 异内容 409。
  - 重启恢复：pending/applied 从事件重建，无新表。

改动文件与范围偏差：
  - factory/control/run_routes.py — follow_up ACTIVE 分支、get_run、_followup_status 辅助；新增 uuid/now 导入
  - factory/control/run_lifecycle.py — 新增 _consume_pending_followups；clarify/continue_run 调用消费
  - frontend/src/conversation/run-state.ts — 新增 FollowUpBadge/FollowUpStatus/followUpBadge/FOLLOWUP_BADGE_LABEL；followup composer hint 更新
  - frontend/src/conversation/RunWorkspace.tsx — ThreadMessage 接受 followups 参数并渲染徽标；queuedNote 文案更新
  - frontend/src/workspace/types.ts — 无结构变更（pending_id 最终未加，因 store.conversation 不传递）
  - 范围偏差：无。所有改动在允许范围内。service.py 无需改动（消费逻辑自包含在 run_lifecycle.py 内）。

检查（命令 / 环境 / 退出码 / 日志路径）：
  1. uv run pytest tests/test_run_followup.py -q → 16 passed, 退出码 0
  2. cd frontend && npm test -- src/conversation/RunWorkspace.test.tsx src/conversation/run-state.test.ts → 23 passed (11 + 12), 退出码 0

实际点击与截图（真实服务或测试夹具）：
  - 所有后端测试使用 app_env fixture（真实 SQLite + FakeSDK + TestClient），非 mock；状态转换经真实 store.update/export_events 验证。
  - 前端测试使用 vitest + @testing-library/react + jsdom，mock API 层验证渲染逻辑。

仍未验证的能力：
  - 真实模型执行下的 worker 跑到 needs_human 后自动消费场景（需要真实 SDK runner 或集成环境）。
  - 前端 CSS 样式（.cv-followup-badge）未定义，仅有类名输出；视觉效果待 CSS 补充。
  - "取消中"与"已取消"的前端视觉区分仅依赖后端 status=cancelled，无 cancel_requested 中间态；如需独立 UI 区分需额外后端支持。

下一任务需要知道的事实：
  - store.conversation() 不传递 pending_id 到前端；每条消息的 applied 字段来自 followups 全局列表而非消息粒度匹配。如需精确到消息的 applied 状态，需改 store.py（当前不在 T03 范围内）。
  - _consume_pending_followups 在 svc.lock 下运行，依赖 export_events 的 kind 过滤，不新增表。
  - followup.pending 的 id 是 uuid.uuid4().hex（32 字符 hex），与 run id 格式一致。
