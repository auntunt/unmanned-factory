# N8：固定测试地址字段（D6 唯一代码缺口）

基线 6fe1883。worktree `/Users/auntlee/workspace/.factory-worktrees/webuddy-next-n8`，分支 `webuddy-next-n8`。

## 用户结果
管理员登记部署目标时可以填写「固定测试地址」；用户在成果上能看到并点开这个地址。

## 依据（已锁定，不要重新设计）
- README 行「完整软件交付」：需求→编码测试→GitHub→**公司固定测试地址**→实际核心操作；继续修改后同地址更新。
- 共识 2026-09-17 决策：域名范围为公司域名下测试地址，部署范围为**公司已有测试服务器**。
- 主会话裁决：「固定测试地址」= `deploy_targets` 上的一个可展示 URL 字段。**非 SSH 的静态站/Vercel 等部署形态不在本轮范围**，不要为它们设计抽象。
- 调查报告 `docs/implementation/webuddy-next/survey/D6.md`：现有 GitHub 绑定/发布/部署 SSH 执行/版本可追溯**都已实现且无 mock**，唯一代码缺口就是这个可展示地址字段。不要重做已实现的段落。

## 范围
1. `deploy_targets` 的 data 增加可选字段 `service_url`：仅接受 http/https 绝对 URL，非法值拒绝并给结构化错误；空值合法（表示未登记）。
2. `ServerTargets.tsx` 管理员表单可填写/编辑/清空该字段，展示时原样显示。
3. 后端返回该字段（沿用现有 deploy-targets 读接口的 admin-only 边界，不放宽）。
4. 项目绑定查询（member 可见那条，`remote_routes.py:95` 一带）：**允许** member 读到已绑定目标的 `service_url`（用户要能打开地址），但**不得**因此暴露 host/user/port/commands/指纹等其它配置。这条要有测试。

## 范围外（不做）
- 不改 SSH 执行、health_check、rollback、幂等收据逻辑。
- 不改 GitHub 发布链路。
- **不要碰 `frontend/src/workbench/Deliverables.tsx`**（N6 占用）——成果页的地址展示由 N6 消费你这个字段，你只要把字段和接口做出来，并在回执里写清最终字段名和返回位置。
- 不设计蓝绿/多环境/版本化地址。

## 允许修改的文件（越界先回报主会话）
- `factory/control/deploy_targets.py`
- `factory/control/remote_routes.py`
- `frontend/src/workbench/ServerTargets.tsx`
- 对应测试（`tests/`、`ServerTargets.test.tsx`）

## 主会话保留（禁止修改）
`Deliverables.tsx`、`App.tsx`、`Workbench.tsx`、`index.css`、`workspace/types.ts`、`factory/control/app.py`

## 必须有测试
1. 合法 http/https URL 可存可读。
2. 非法值（非 URL、javascript: 之类、相对路径）被拒绝并返回结构化错误。
3. 空值合法，读回为空。
4. member 通过项目绑定查询能读到 `service_url`，但读不到 host/user/port/commands/指纹。
第 4 条做**变异验证**（去掉字段裁剪确认测试变红），红→绿输出写进回执。

## 验证（真跑，记录命令与退出码）
- `uv run pytest tests -q -k "deploy or remote or target"`
- `cd frontend && npx tsc --noEmit` + `npx vitest run src/workbench/ServerTargets.test.tsx`

## 交接
回执 `docs/implementation/webuddy-next/receipts/N8.md`：实际模型、提交 sha、**最终字段名与它出现在哪些响应里**（N6 要照着接）、验证命令+退出码、变异验证红→绿、未验证项。
