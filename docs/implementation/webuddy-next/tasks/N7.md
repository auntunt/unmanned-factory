# N7：非 general 操作的 delivery_type 写入（D1 需修复项 1）

基线：集成分支当前 HEAD。worktree `webuddy-next-n7`。

## 用户结果
bugfix / release / startup / dependencies 这些操作产出的成果，交付类型不再永远显示「未声明」。

## 依据（已锁定）
- 调查报告 `docs/implementation/webuddy-next/survey/D1.md`：非 general 操作不经 requirement_analysis，`delivery_type_inferred` 从未写入，前端始终显示「未声明」。修复落点：`run_routes.py` 的 `new_run()` 补调 `infer_delivery_type_from_text`。
- r2 的 T09/T12 已定推导规则：**唯一正向命中才定型**，non_goals 负向减除只用 form 层，歧义返回 None 保守不猜。**这套规则不许改**，你只是把它接到这些操作上。
- 未命中就是「未声明」，**不要为了消灭空值而瞎猜一个类型**。

## 范围
在 `run_routes.py` 的 `new_run()`（非 general 操作路径）调用既有 `infer_delivery_type_from_text`，唯一命中才写 `delivery_type_inferred`，歧义或未命中不写。

## 范围外
- 不改推导规则本身（关键词表、词边界、form/deploy 分层、negative 减除）。
- 不改 general 操作既有路径的行为。
- 不改前端（展示已存在且有测试）。
- 不碰 `run_execution.py` / `requirement_analysis.py`。

## 允许修改
`factory/control/run_routes.py`、`tests/` 下对应测试。

## 主会话保留（禁止修改）
所有前端文件、`app.py`、`run_execution.py`、`requirement_analysis.py`、`session_skills*.py`

## 必须有测试
1. bugfix/release/startup/dependencies 各一条：文本唯一命中 → 写入正确 delivery_type。
2. 歧义文本 → **不写**（保持未声明），不是写个默认值。
3. general 操作路径行为不变（既有测试仍绿）。

## 验证
`uv run pytest tests -q -p no:randomly -k "delivery or run_routes or spec"`

## 交接
回执 `docs/implementation/webuddy-next/receipts/N7.md`。
