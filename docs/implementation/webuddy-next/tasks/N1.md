# N1：运行环境可用性的角色边界（D4 P1/P2/P3/P4 + M1）

基线 6fe1883。worktree `/Users/auntlee/workspace/.factory-worktrees/webuddy-next-n1`，分支 `webuddy-next-n1`。

## 用户结果
普通成员只看到「当前能不能用 / 能不能部署」以及缺口说明，看不到完整运行环境配置；管理员看到完整配置。

## 依据
- README 行「配置归管理员」：普通用户只看能否使用或部署。
- 共识 §20：系统依据管理员配置判断可用性，不能把资源配置表单转交普通成员。
- 调查报告 `docs/implementation/webuddy-next/survey/D4.md` 的 P1/P2/P3/P4/M1。

## 范围
1. `GET /api/v2/runtime` 按角色裁剪：admin 返回原样；member 只得到可用性摘要（readiness 布尔 + blockers 文案），不含 profiles 明细、tools 诊断、host 信息、auth_sources。
2. 新增 member 可用的 readiness 端点（或在同一端点按角色裁剪，二选一，你定，但必须只有一份真相来源）。
3. `PUT /api/v2/runtime/profiles`、`PUT /api/v2/runtime/operations`、`POST /api/v2/runtime/probe` 补路由层显式 admin 检查（中间件已拦截，这是双保险，不改变行为）。
4. 前端：member 能看到可用性指示与缺口文案。

## 不做
- 不改中间件 `app.py:230` 的白名单逻辑。
- 不新增任何配置写接口，不碰 deploy-targets / team / GitHub 发布。
- 不做管理员配置对话（那是另一单 N2）。
- 不改密钥存储与脱敏链路。

## 允许修改的文件（写入边界，越界先回报主会话）
- `factory/control/runtime_routes.py`
- `factory/control/runtime.py`（只为提取 readiness 摘要，不改探针与 profile 语义）
- `frontend/src/workbench/RuntimePage.tsx`
- 新建 1 个 member 可用性展示组件文件（自取名，放 `frontend/src/workbench/`）
- 对应测试文件（后端 `tests/`，前端 `*.test.tsx`）

## 主会话保留（禁止修改，需要接线就回报，我来接）
`frontend/src/App.tsx`、`frontend/src/workbench/Workbench.tsx`、`frontend/src/index.css`、`frontend/src/workspace/types.ts`、`factory/control/app.py`

## 完成条件
- member 身份访问 `GET /api/v2/runtime` 拿不到 profiles/tools/host/auth_sources（有测试断言）。
- member 能拿到 readiness 与 blockers（有测试断言）。
- admin 行为不变（原有测试仍绿）。
- 三个写端点的显式 admin 检查各有一条测试。
- 后端定向：`uv run pytest tests -q -k "runtime"` 全绿并记录实际命令与退出码。
- 前端：`npx tsc --noEmit` 干净 + 相关 vitest 通过。
- 不新增假成功：缺口文案必须来自真实 blockers，不能硬编码。

## 交接
回执写 `docs/implementation/webuddy-next/receipts/N1.md`：实际模型、提交 sha、改动文件、命令+退出码+结果、需要主会话接线的点、未验证项。
