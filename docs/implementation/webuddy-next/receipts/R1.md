# R1 回执：会话 Skill 正文保存与实际装载

模型: claude-opus-4-6[1m]
基线: 4d2e3f8
分支: webuddy-next-r1

## 提交

- `dbb2b7e` fix(session-skill): 保存 ZIP 正文并通过 reference_mount 装载到 runner
- `3232484` test(R1): 补全 5 条完成条件测试

## 正文存储

`session_skill_bodies` 表（`session_skills.py`），与 `session_skills` 同库，
`skill_id` 主键关联 `session_skills.id`。ZIP 导入原样存储；GitHub 导入用
`FetchResult.content` 构建 ZIP 再存储（确定 commit，不运行时重拉）。

## 装载通道

`compile_mounts` 中 `session_skill_snapshot` 段
（`mounts.py:161-195`），走既有 `compile_mounts` 的 `append_document` 逻辑，
trust 标记为 `reference_data`，复用已有归属校验。不另造装载机制。

## 两条链路接入点

| 链路 | 文件:行号 |
|------|-----------|
| 无项目 do 聊天 | `agent_routes.py:358-361`（freeze + 传入 compile_mounts） |
| coding 运行 | `run_execution.py:38-43`（freeze snapshot）→ `run_execution.py:77`（compile_mounts 透传） |

## 红→绿原始输出

### 红（基线 4d2e3f8，修改前）

```
FAILED docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_session_skill_body_reaches_chat_runner
>       assert MARKER in json.dumps(asdict(seen[0]),ensure_ascii=False), 'Imported SKILL.md body never reaches the runner prompt or reference mount'
E       AssertionError: Imported SKILL.md body never reaches the runner prompt or reference mount
1 failed, 2 warnings in 1.47s
```

### 绿（修改后）

```
docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_session_skill_body_reaches_chat_runner PASSED
1 passed, 2 warnings in 0.79s
```

## 变异验证输出

`test_capability_loaded_mutation_no_mount_evidence` — 去掉 mount 证据
（mount_snapshot.documents 为空），session_skill 不出现在 loaded 中。

`test_capability_loaded_mutation_no_mount_snapshot` — 完全无 mount_snapshot，
同样不出现。

两条变异测试均绿（即：移除证据后 loaded 正确为空）。

## 5 条完成条件

| # | 条件 | 状态 |
|---|------|------|
| 1 | Codex 复现由红转绿 | PASS |
| 2 | coding 链路装载测试 | PASS (`test_coding_path_mounts_session_skill_body`) |
| 3 | 解绑后原快照不变 | PASS (`test_unbind_preserves_existing_snapshot`) |
| 4 | capability_sources.loaded 变异验证 | PASS (2 条变异测试) |
| 5 | 注入正文不被当作指令 | PASS (`test_injection_body_is_reference_data_not_instructions`) |

## Codex 复现文件全量结果

```
1 passed (R1), 2 failed (R2/R3 预期红)
```

## 阻塞 / 越界

无。所有修改在任务单允许的文件范围内。
