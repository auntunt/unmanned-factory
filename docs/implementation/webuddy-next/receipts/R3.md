# R3 回执：配置对话作业状态生命周期

模型：claude-opus-4-6[1m]（Opus 4.6）  
提交：`03635ad` on `webuddy-next-r3`  
基线：`4d2e3f8`

## 生命周期改动位置

| 改动 | 文件:行号 |
|------|-----------|
| pending 在派发前落库 | `factory/control/agent_routes.py:721-732` |
| answer() 就地更新 pending→completed | `factory/control/agent_routes.py:754` (`_update_admin_config_msg`) |
| _admin_config_error 区分取消/失败，就地更新 | `factory/control/agent_routes.py:780-784` |
| _update_admin_config_msg（新增公用更新函数） | `factory/control/agent_routes.py:765-777` |
| _resolve_stale_pending（GET 端恢复残留） | `factory/control/agent_routes.py:613-645` |

## 红→绿：Codex 复现

### 红（基线 4d2e3f8）

```
$ uv run --extra codex pytest -q -p no:randomly docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_completed_admin_answer_clears_pending
F                                                                        [100%]
FAILED docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_completed_admin_answer_clears_pending
  assert not any(m.get('status') in ('pending','running') for m in conv['messages']),
    'Completed job leaves permanent pending message, frontend never stops polling'
  AssertionError: Completed job leaves permanent pending message, frontend never stops polling
1 failed, 2 warnings in 1.44s
```

### 绿（03635ad）

```
$ uv run --extra codex pytest -q -p no:randomly docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_completed_admin_answer_clears_pending
.                                                                        [100%]
1 passed, 2 warnings in 0.85s
```

## 变异验证：pending 派发前落库

删除 `_save_admin_config_conv(conv)` (行 732) → `test_pending_persisted_before_dispatch` 变红：

```
$ uv run --extra codex pytest -q ... ::test_pending_persisted_before_dispatch
E  AssertionError: MUTATION CAUGHT: pending message must be in DB before runner starts
1 failed in 5.81s
```

恢复后测试通过。

## 5 条完成条件

| # | 条件 | 状态 |
|---|------|------|
| 1 | Codex 复现由红转绿 | PASS |
| 2 | 失败路径：runner 抛错 → pending 变 failed 终态 | PASS (`test_failed_runner_marks_pending_as_failed`) |
| 3 | 取消/中断：有真实终态 | PASS (`test_cancelled_job_has_terminal_state`) |
| 4 | pending 派发前落库 + 变异验证 | PASS (`test_pending_persisted_before_dispatch` + mutation red) |
| 5 | 前端：完成→停止轮询→调用 onConfigChanged | PASS (`AdminConfigChat.test.tsx:5th test`) |

## 前端行为验证

```
$ cd frontend && npx vitest run src/workbench/AdminConfigChat.test.tsx
✓ src/workbench/AdminConfigChat.test.tsx (5 tests) 38ms
Test Files  1 passed (1)
Tests  5 passed (5)
```

## npm run build

```
$ cd frontend && npm run build
✓ built in 1.49s
退出码 0
```

## tsc --noEmit

```
$ cd frontend && npx tsc --noEmit
退出码 0
```

## 未验证 / 阻塞

- `test_admin_config_surface_gate` 4 条测试因 ModuleNotFoundError 失败——基线即存在，与 R3 无关。
- Codex 另外两条测试（R1/R2）仍红，符合预期。
- 无越界改动。
