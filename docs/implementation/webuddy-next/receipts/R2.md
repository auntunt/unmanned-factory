# R2 回执：会话 Skill 访问授权

## 模型
claude-opus-4-6[1m]

## 提交
- `12bb5c4` fix(auth): 会话 Skill 三端点统一验证会话归属
- `f81a50c` test(auth): R2 完成条件 5 条测试 + 合约测试归一化
- `3e53c7a` docs: 复制 R2 任务单与 Codex 复现文件

## 授权判定点
`factory/control/session_skill_routes.py:24-31` (`_verify_session_owner`)

逻辑：`SELECT data` 取 conversation JSON → 解析 `actor_id` → 管理员放行 → 否则 `str(actor_id) != str(user['id'])` → 403。

## 窄接线
`factory/control/app.py:239-245`

中间件白名单新增 `session_skill_action`：仅匹配 `r'/api/v4/sessions/[^/]+/skills(?:/[^/]+)?'`。
命中时 `pass`，归属验证完全由路由处理器执行。不影响其他 v4 写端点的 admin-only 策略。

## 红→绿原始输出

### 红（修复前）
```
FAILED docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_member_cannot_read_another_users_session_skills
E       AssertionError: Unrelated member reads session metadata: 200 {"items":[{"id":"17b398360bdf45fd8fdb157b2703d32a","session_id":"86f9c4078d0142eea372c5b2680de2f9","name":"company-rule","origin":"zip","source_ref":"SKILL.md","source_version":"","source_sha256":"9bb6d6b7d2914ee5954590703295ae2739d34fa7f217822c349e53702698194c","entry":"SKILL.md","description":"innocuous metadata","dependencies":[],"import_state":"imported","dependency_state":"ready","actor_id":"1","created_at":"2026-09-19T05:36:12.248944+00:00"}]}
E       assert 200 in (403, 404)
1 failed, 2 warnings in 1.56s
```

### 绿（修复后）
```
docs/acceptance/webuddy-next-2026-09-19/test_codex_review.py::test_member_cannot_read_another_users_session_skills PASSED
1 passed, 2 warnings in 0.89s
```

## 变异验证输出
`tests/test_session_skill_auth.py::TestNarrowWhitelist::test_mutation_wide_whitelist_breaks_test`
- 源码审计：确认 `session_skill_action` 正则仅含 `/sessions/` 路径
- 确认无 `path.startswith('/api/v4')` 或 `r'/api/v4/.*'` 通配放行
- 行为验证：member POST `/api/v4/agents` → 403，POST `/api/v4/skill-ingestions` → 403
- 测试结果：PASSED

## 5 条完成条件
1. Codex 复现红→绿：通过
2. 无关 member GET/导入/删除他人会话全部 403：通过（3 条测试）
3. 普通 member 能导入自己的会话：通过
4. 窄授权没有放开别的 v4 写接口（含变异验证）：通过
5. 授权先于外部拉取（fetcher 调用次数 = 0）：通过

## Codex 全量复现（3 条）
```
FAILED test_session_skill_body_reaches_chat_runner — R1 的活，预期红
PASSED test_member_cannot_read_another_users_session_skills — R2，已绿
FAILED test_completed_admin_answer_clears_pending — R3 的活，预期红
```

## 阻塞
无。
