# N7 回执：非 general 操作的 delivery_type 写入

## 改动

- `factory/control/run_routes.py` L124-128（+5 行）：`new_run()` 在 `create_run` 之后、`start_plan` 之前，对 `body.operation != 'general'` 的路径调用 `infer_delivery_type_from_text(body.request)`，唯一命中写入 `delivery_type_inferred`，歧义/未命中不写。
- `tests/test_delivery_type_non_general.py`（新增 63 行）：6 条测试覆盖 bugfix/release/startup/dependencies 唯一命中、歧义不写、general 不变。

## 未改动

推导规则（关键词表、词边界、form/deploy 分层、non_goals 减除）、前端、`app.py`、`run_execution.py`、`requirement_analysis.py`、`session_skills*.py`、`conversation_tools.py` 均未触碰。

## 验证

```
uv run pytest tests -q -p no:randomly -k "delivery or run_routes or spec"
182 passed, 2371 deselected    退出码 0
```

## 提交

`8702282` on `webuddy-next-n7`
