# N11 回执：管理员配置对话入口

## 模型
claude-opus-4-6[1m]

## 提交
- `38df1b2` feat(N11): 管理员配置对话后端——专用端点 + 服务端角色判定 + 7 条测试
- `1994faf` feat(N11): 前端配置对话面板——嵌入 RuntimePage 右栏

## 入口形状
- 路由：`POST/GET /api/v4/admin-config/conversations`、`POST /api/v4/admin-config/conversations/{cid}/messages`
- 组件：`AdminConfigChat.tsx`，挂载在 `RuntimePage.tsx` 右栏顶部（`<AdminConfigChat />`），与表单并列
- 不新增顶层导航、不新增顶层路由

## 5 条测试（7 个函数）
1. `test_admin_config_conversation_has_config_tools` — admin 走配置入口 → binding admin_config=True，MCP 清单含 9 个配置工具 ✅
2. `test_member_config_conversation_rejected` + `test_member_cannot_send_to_admin_config_conversation` — member → 403，不产生模型调用 ✅
3. `test_client_lie_admin_config_ignored` + `test_client_lie_admin_flag_in_normal_conversation` — 变异验证：client 谎报 admin_config=True 被忽略（中间件 403 + 闸门 calc/export only） ✅
4. `test_config_change_reflected_in_form_read` — AdminConfigTools 改 runtime → form 读 API 返回改后值 ✅
5. `test_normal_do_session_unaffected` — 普通 do 会话 binding admin_config=False ✅

## 第 3 条变异验证结果
- 层 A：member POST 到 `/api/v4/admin-config/conversations` 无论请求体含什么字段 → 中间件 403
- 层 B：即使 `binding_for(admin_config=True)` + `actor_role='member'`，`create_server` 闸门仍阻止配置工具注册，清单 = `{calc, export}`

## 验证命令 + 退出码
```
uv run --extra claude pytest tests/test_admin_config_surface_gate.py tests/test_agent_chat_capability.py tests/test_admin_config_tools.py -q -p no:randomly
  → 54 passed, exit 0

uv run --extra claude pytest tests -q -p no:randomly -k "conversation or agent_routes or runtime"
  → 84 passed, exit 0

cd frontend && npx tsc --noEmit
  → exit 0

cd frontend && npx vitest run
  → 49 files, 383 passed, exit 0

cd frontend && npm run build
  → tsc -b + vite build, exit 0
```

## 阻塞
无。`.venv` 缺 `mcp` 模块需要 `--extra claude`（非本单 bug，任务单已注明）。

## 修改清单
- `factory/control/agent_routes.py` — 新增 AdminConfigMessage 模型 + 4 个端点
- `frontend/src/workbench/AdminConfigChat.tsx` — 新组件
- `frontend/src/workbench/RuntimePage.tsx` — import + 挂载 AdminConfigChat
- `frontend/src/workbench/detail.css` — .wb-config-chat-* 样式
- `tests/test_admin_config_conversation.py` — 7 条测试

## 未修改（红线）
conversation_tools.py、admin_config_tools.py、app.py、App.tsx、Workbench.tsx、index.css、workspace/types.ts、runtime_routes.py
