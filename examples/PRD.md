# 文本工具需求

给一个小型文本处理模块定的验收标准。任务 YAML 用 `spec_ref` 引这里的编号，
`spec_doc` 指向本文件 —— 规格监工拿到的是**下面这些正文**，不是编号本身。

## 验收标准

- AC-1: slugify(s) 返回小写、以 `-` 连接的 ascii slug；
  连续空白折叠成一个 `-`；
  首尾不留 `-`
- AC-2: 非 ascii 字符按 NFKD 分解后丢掉无法转写的部分，不抛异常
- AC-3: 空串和纯符号串返回空串，不返回 None

## 非目标

- 不做多语言音译（中文转拼音之类），AC-2 只要求不炸
